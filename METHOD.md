# METHOD — 這套系統怎麼做出每一筆預測

**這份文件是給「沒有前一次對話記憶」的人（或 Claude）讀的。**
目標是：只讀這一份，就能重現系統裡每一種預測，並知道每個常數是怎麼來的。

程式碼是唯一的事實來源，這份文件標明每一段的位置。
**若文件與程式碼不符，以程式碼為準，並回來修這份文件。**

---

## 0. 系統一共產出幾種預測

每個交易日，對每一檔標的、每一個期間，各家模型各出一筆，全部寫進
`data/predictions.jsonl`（append-only）。

| 家族 | 模型 | 期間 | 市場 | 誰產生 |
|---|---|---|---|---|
| baseline | `always_up`／`random`／`momentum` | 5、20 | TW、US | 機器，每日 |
| stat | `stat_logit`／`stat_gbdt` | 5、20 | TW、US | 機器，每日（資料不足則不出手） |
| claude | `claude` | 5、20 | TW、US | 人／Claude 開 session 寫判斷檔 |
| claude | `claude` | **250** | TW、US | 建置一次，之後每日滾動（見 §5） |
| llm | `llm_*` | 5、20 | TW、US | 設了 API key 才跑（`src/models/llm.py`） |

期間的常數在 `src/config.py`：`HORIZONS = [5, 20]`（統計模型與基準線）、
`HORIZON_1Y = 250`（一年期，刻意獨立，因為它不進統計模型）。

**身分鍵** `pid = (as_of, horizon, model, code)`，**不含模型版本** ——
含版本等於替同一個邏輯鍵重開一個槽，改模型就能洗掉舊成績。
修訂以 `revision` 遞增並存，計分時取每組最新一筆。

---

## 1. 所有模型共用的數學

### 1.1 期望值
```
EV = P(漲) × 上漲時幅度 + (1 − P(漲)) × 下跌時幅度
```
**只看方向機率會系統性誤導**：P(漲)=0.6 但漲 1%／跌 3% 的標的，EV 是負的。
面板的排序一律用 EV，不用 P(漲)。

### 1.2 分位數走對數常態（`src/models/quantiles.py`）
```python
Z10 = 1.36
ratio_mean = max(1 + EV, 1e-6)
m    = ln(ratio_mean) − σ²/2
q10  = exp(m − z·σ) − 1
q90  = exp(m + z·σ) − 1
```
**為什麼不用常態**：常態的左尾延伸到 −∞，而報酬不可能低於 −100%。
一年期又高波動時會算出**負的股價**（實測南電 −316.5 元、創意 −914.7 元，
38 檔裡 18 檔的 q10 < −90%）。對數常態保證 q10 > −100%。

**兩個 z 值是刻意不同的，不要「統一」掉**：
- 統計模型與基準線用 `z = 1.2816`（常態 10%／90% 的精確分位）
- Claude 判斷用 `Z10 = 1.36`（加寬，涵蓋「我對 P漲 的估計本身也不確定」）
  把它改回 1.2816 會**靜默地把判斷的區間縮窄**，讓校準看起來更好而實際更差。

### 1.3 幅度（**只適用 h = 5、20**；h = 250 見 §5.5）
```
base = 0.85 × vol × √h
up   = base × (1 + skew)
dn   = −base × (1 − skew)
skew ∈ [−1, 1]，正值＝上檔比下檔大
```
- `0.85` 是把「未來實現波動」折到「條件期望幅度」的係數。
- `vol` 用 `vol_20`，面板顯示的波動也用它，否則同一張卡上會出現
  「±33% 幅度」配「18% 年化波動」的自相矛盾。
- **√h 外推站得住**：實測變異比 VR(h) 在 h=5~120 都落在 0.96–1.00，無顯著均值回歸。
- **已知的小不一致**：這裡的期望值走兩點模型、區間走對數常態，兩者對 P漲 的
  隱含值在 h=20 差 0.01–0.03。`docs/METHOD.md`（舊）記載的決定是：等有結算資料、
  看過 Mincer–Zarnowitz 斜率再決定是否校正。h=250 的差距是 0.09–0.15，
  那個不能等，已在 §5.5 改掉。

---

## 2. 基準線（`src/models/baselines.py`）

**基準線是判斷其他模型有沒有價值的唯一標尺，所以它們一旦上線就凍結。**
即使發現它們的某個細節不夠好（例如下面的分位數用常態而非對數常態），
也不要「順手修好」—— 改了基準線，之前所有的比較就不可比了。
要改就當成新模型另開一個名字。

| 模型 | P(漲) | 期望報酬 | 分位數 |
|---|---|---|---|
| `always_up` | 固定 **0.55** | as_of 之前已實現的 h 期報酬**無條件平均** | ±0.08（固定） |
| `random` | `sha256(as_of\|code\|h)` 前 4 bytes / 2³² | `(p − 0.5) × 0.10` | ±0.08（固定） |
| `momentum` | `clip(0.5 + tanh(ret_20 × 5) × 0.12, 0.05, 0.95)` | 歷史均報酬 `+ tanh(ret_20 × 4) × 0.03` | `exp ± 1.2816 × vol_20 × √h`（**常態**，見上） |

| `mech_core` | `clip(該市場歷史基本率 + tilt × 0.12, 0.30, 0.70)`，<br>`tilt = 0.5×(−ret_20 分位) + 0.5×(rev_yoy 分位)` | `0.85 × vol_20 × √h × (2p−1)`（skew=0） | 對數常態，z=1.2816 |

`random` 用雜湊當種子而非 `Math.random()` —— 重跑必須長一樣，否則不可稽核。

**`mech_core` 是手寫判斷的機械核心**（2026-09-19 新增，新增不等於修改既有三條線）。
2026-09-18 台股手寫 p20 與 `ret_20` 的 Spearman 是 −0.62、與 `rev_yoy` 是 +0.62，
而這條線與手寫 p20 的 Spearman 達 **+0.736** —— 判斷有三分之二可被「跌深、營收強」
兩句話複製，且與 `momentum` 恰好是鏡像。**判斷要證明有增量價值，必須贏過這條線。**

它的兩半歷史證據強弱相反（PIT 宇宙、台股 511 個交易日、重疊調整後 t）：

| 訊號 | h=5 | h=20 | h=60 |
|---|---|---|---|
| 營收年增 | IC +0.069（t +2.69） | +0.129（t +2.42） | +0.188（t +2.32） |
| 反轉（−ret_20） | IC −0.045（t −1.65） | −0.074（t −1.54） | −0.042（t −0.46） |

營收年增是唯一 |t| > 2 的；反轉三個期間 IC 全為負。仍保留 0.5/0.5 是因為
這條線的工作是**複製判斷在做的事**，不是做一條最好的線。詳見 `LESSONS.md`。

**`always_up` 不好打敗，而那正是重點。** 回測裡 5 日 58.6%、20 日 68.4%
是大多頭的基本率；`stat_logit` 的 63% 單看體面，沒有基準線就會被誤判成有效。

---

## 3. 統計模型（`src/models/statistical.py`）

```
訓練集 = 該市場 panel 中 date ≤ (as_of − h 個交易日) 的所有列
         （不是 as_of！否則標籤的一部分落在 as_of 之後 ＝ 未來函數）
最低訓練列數 MIN_TRAIN_ROWS = 2000，不足就不出手（回傳空表）
每個市場各訓練一個模型，結果再併回來（2026-09-19 起，VERSION 1.3.0）
該市場覆蓋率 < MIN_FEATURE_COVERAGE = 0.2 的特徵整欄剔除
```

**為什麼必須分市場訓練**：美股沒有法人買賣超、融資券、月營收，
`foreign_5`／`foreign_20`／`trust_5`／`rev_yoy`／`margin_chg_5`／`margin_chg_20`／`short_ratio`
七個特徵 100% 缺值。混訓時 imputer 的中位數來自台股列（台股列數是美股的 1.7 倍），
於是每一檔美股都被餵進「台股中位數的月營收年增與外資買超」——
而 `rev_yoy` 在多變量 Fama-MacBeth 裡的係數是 +3.28%（t=5.26）。
`turnover_20` 更是台幣十億與美元十億混在同一欄（差約 30 倍幣值）。
分開之後美股用 12 個特徵、台股 19 個，基本率也各自歸位：
20 日基本率台股 61.9%／美股 56.9%（混訓時是 59.6%），
平均報酬 +5.56%／+2.18%（混訓時 +4.09%）。

**為什麼要明確剔除低覆蓋特徵，而不是交給 imputer**：全空的欄位 sklearn 會跳過，
行為正確但靜默；**幾乎**全空的欄位更危險 —— 只要有一個非空值，中位數就等於那個值，
整欄變成常數而模型照樣為它擬合係數。

**版本切換的處置**：`_pid` 不含模型版本（憲法 B），所以 2026-09-16～18 以 1.2.0
（混訓）寫下的預測依憲法保留並照常結算。`settle.py` 自 2026-09-19 起把
`model_version` 寫進結算記錄，`score.py` 在同一模型名出現多個版本時另外分列
（`stat_logit@1.2.0` / `stat_logit@1.3.0`），否則兩套不同的方法會被算成同一個平均。

| 模型 | 方向 | 期望報酬 | 分位數 |
|---|---|---|---|
| `stat_logit` | `LogisticRegression(C=0.3)`（含標準化 pipeline） | `Ridge` | 殘差標準差的對數常態近似，**按各股波動度縮放** |
| `stat_gbdt` | `HistGradientBoostingClassifier` | `HistGradientBoostingRegressor` | 分位數回歸 q=0.10／0.90，取 min/max 保證不交叉 |

**`stat_logit` 的分位數為什麼要按個股波動縮放**：原本用單一純量 σ，
導致中華電（日波動 0.48%）與大立光（5.91%）拿到完全相同的 35.2% 區間；
條件覆蓋率從最低波動組 99% 到最高波動組 65%（名目 80%）。
現在的作法是把殘差除以各自的波動度得到無單位尺度 `k`，再乘回每檔自己的波動度。

**h=250 刻意不進統計模型**：兩年面板在該尺度上每檔只有約 2 筆獨立觀測，
擬合出來的係數是雜訊。一年期只做判斷＋檢查點對帳。

---

## 4. Claude 判斷 · 5 日／20 日

**這是唯一無法自動化的部分**，流程寫在 `DAILY.md` §二。方法本身如下。

### 4.1 產出格式
每天寫一個 `judgments/build_<as_of>.py`（台股）與 `build_us_<as_of>.py`（美股）。
**結構照最近一份複製**（目前 `judgments/build_20260918.py`）。
核心是一個手寫的清單，每一列是 `(code, p20, skew, conviction, why)`。

### 4.2 機率
`p20` 是**手寫的 20 日上漲機率**，由當日四面向簡報（`data/briefing.parquet`）推導。
其他期間由 √t 縮放導出，不另外手寫：
```python
p_h = 0.5 + (p20 − 0.5) × √(h / 20)
```
所以 5 日的偏離幅度是 20 日的 1/2。**不要對 5 日與 20 日各寫一組 p** ——
那會讓兩個期間的判斷不一致，而不一致無法歸因。

### 4.3 錨點（**會隨總體環境改變，每次都要查現值**）
`p20` 的中性值不是 0.50。目前（2026-09-18 起）：

| 市場 | 錨點 | 依據 |
|---|---|---|
| 台股 | **0.53** | 升息循環重啟後由 0.58–0.68 下修；高於美股是因為本地 AI 硬體循環有可查證的訂單在推，與 Fed 的關聯較弱 |
| 美股 | **0.52** | Fed 於 2026-09-16 升息一碼至 3.75–4%、10 年期公債殖利率 5.01% |

**歷史錨 0.58–0.68 已作廢**：它來自一段全程多頭、且實證其獨立樣本數約等於 1 的樣本。
錨點每次調整都要寫進當天的 build 檔頂端並記入 `LESSONS.md`，
因為它是整批判斷的共同平移項，影響大於任何單一個股的判斷。

**錨點必須以 `anchor` 欄位寫進判斷檔**（2026-09-19 起），`ingest_judgment.py`
會把它帶進 `predictions.jsonl` 的每一列。理由是計分：手寫 p20 的橫斷面標準差
台股只有 0.027、美股 0.020，而錨點是 0.53／0.52 —— 每一筆判斷有 95% 以上的
內容來自錨點。不把它分出來，準確率與 Brier 量到的幾乎全是「錨點對不對」，
而錨點一天只有一個決定、選股一天有 50 個，兩者累積統計證據的速度差約 50 倍。
拆解由 `src/attribution.py` 做，`daily.py prepare` 會在結算後印出：

| 分項 | 預測 | 對照 |
|---|---|---|
| 市場判斷 | 錨點 − 0.5 | 當日該市場實際上漲比率 − 0.5 |
| 選股判斷 | p − 該批錨點 | 個股報酬 − 當日該市場平均報酬（去均值） |

去均值之後市場整體漲跌對選股分數毫無貢獻，兩項因此互不污染。
台美**分開**計算橫斷面 —— 兩地的交易日與漲跌互不相干，混在同一個橫斷面是錯的。
有效天數 = 名目天數 ÷ 重疊倍數（日頻預測、h 日期間，相鄰重疊 (h−1)/h）。

### 4.4 skew 與幅度
`skew` 手寫，幅度照 §1.3 用 `vol_20`。分位數用 `Z10 = 1.36`。

### 4.5 寫 `why` 的規則（這些是審核抓過的錯誤所定下的）
- 每個引用的數字都要能在 briefing 裡對得上，尤其「最高／最低／唯一／全場」。
- **反向證據必須並陳**（LESSONS D1）：只寫支持方向的證據＝單邊採證。
- **極值要先驗真假**：`margin_chg` 之類的比率要同時看絕對量與該檔自身的中位數
  （第一金 +599% 是真的、台灣大 +132% 是基數假象）。
- `foreign_5` 是**每日標準化值**：0.85 代表平均每天買超 0.85 倍日均量、
  5 日合計 4.25 倍，不是「買了 0.85 倍」。

---

## 5. Claude 判斷 · 一年期（250 日）

一年期與 5／20 日**不只是期間拉長**：
5／20 日問「這批資金會不會繼續進來」（技術面、籌碼面、動能）；
250 日問「這家公司一年後會不會比現在賺得多，而現在的價格有沒有先反映」
（基本面、公司自己的擴張計畫、產業循環位置）。

### 5.1 兩種論點，證據強度差一個量級
| 類型 | 依據 | 信心 | 程式可否自動改 |
|---|---|---|---|
| **已實查** | 讀過法說會／公司指引／產業資料，`facts` 逐條寫明「誰說的、數字多少」 | medium | **否**，只標記待人工複核 |
| **規則推導** | 只有財報軌跡與估值（`src/models/rule_1y.py`） | low | 是，財報更新時自動重算 |

把沒查過的標的寫得跟查過的一樣有把握，是這套系統最該避免的事。

### 5.2 規則推導的 P漲（`src/models/rule_1y.py`，建置與滾動共用同一份）
```
P漲 = clip(ANCHOR + Σ 百分位(欄位) × 權重, 0.44, 0.64)
ANCHOR = 0.55
權重 = {gm_chg_4q 0.06, roe_ttm 0.04, eps_yoy 0.03, PER 0.04(反向), rev_cagr_3y 0.03}
```
（2026-09-19 前另有 `skew = (P漲 − 0.55) × 1.2`；一年期已無獨立的 skew，見 §5.5。）
- **用橫斷面百分位而非原始值**：第一版用 `eps_yoy × 0.02` 再截斷在 ±0.03，
  而 2026 年多數個股正從低基期回升，年增動輒數百 % —— 13/31 檔的 P漲
  黏在上限。一個對 42% 樣本都生效的上限不是護欄，是模型本身。
- **權重依「多難造假、多慢反轉」給**：毛利率趨勢最高（反映產品組合與定價權，
  一季一季慢慢動）、ROE 次之、EPS 年增再次（易受基期與一次性項目干擾）、
  估值扣分（高本益比＝期望已被計入，一年尺度是風險不是優勢）。
- **`ANCHOR = 0.55` 的由來**：樣本內 250 日為正 79%，但那是兩年單一多頭、
  嚴重高估不可用。改用先驗：大盤 12 個月為正約 65–70%，個股低於大盤
  （Bessembinder 2018 JFE：多數個股長期報酬中位數低於指數），故取 0.55。

#### 週期位置修正（`VERSION = "1.1.0"`，2026-09-19 起）

上一版全部是**橫斷面**百分位，對「這是景氣循環的哪個位置」沒有辨識力。
實例：華邦電與南亞科在記憶體超級循環高點，因毛利率變化第 98 百分位、
EPS 年增第 100 百分位被排到最前段，而同一天手寫的美光論點講的正是這組數字的反面
（「本益比最低的時候最貴，因為分母是週期高點的獲利」）。
兩檔同屬一個循環卻被判到兩端，**差別只在覆蓋率不在判斷**。

```
cyc  = clip((gm_self_pct − 0.75) / 0.25, 0, 1)
CYCLICAL = (gm_chg_4q, eps_yoy, PER) 的貢獻 × (1 − cyc)
P漲 再扣掉 CYC_PENALTY × cyc，CYC_PENALTY = 0.05
```
`gm_self_pct` = 當期毛利率相對該公司**自身歷史**分布的百分位
（擴張視窗、天生 PIT，`SELF_PCT_MIN_Q = 8` 季起才有值）。

- **為什麼是「衰減到 0 再扣分」而不是把權重乘上負數**：第一版寫成
  `mult = 1 − 1.5×cyc`，結果讓「位於自身毛利率高點、但橫斷面成長排名偏低」
  的公司拿到**加分**（實測川湖 +0.020、致茂 +0.022）—— 負貢獻乘負數變正。
  位於自身高點不該是任何形式的優勢。衰減到 0 說的是「這個讀數在週期高點沒有資訊」，
  扣分說的是「均值回歸本身是負面因子」，兩件事分開講才不會互相污染。
- **`roe_ttm` 與 `rev_cagr_3y` 不調整**：前者是水準、後者是三年趨勢，
  對單季循環位置不敏感。
- **效果**（台股 as_of 20260918，24/54 檔受影響，最大降幅 −0.106、最大升幅 0.000）：
  華邦電 0.621→0.515、南亞科 0.631→0.527、台積電 0.605→0.522。
  華邦電的人工修訂值是 0.50 —— 規則現在落在已實查論點附近，那正是目標。
- **美股目前一律不生效**：yfinance 一次只給 5–7 季，不足 8 季，`gm_self_pct` 全為 NaN。
  等 `data/raw/us_fund/` 累積夠了會自動開始作用。

⚠️ **證據狀態（兩半強弱不同，不要一起宣稱）**：

| 問題 | 結果 |
|---|---|
| 毛利率相對自身歷史會均值回歸嗎？ | **會**。Spearman(gm_self_pct, 後四季毛利率變化) = −0.143（台股 52 檔、1,427 個季度觀測）；≥0.95 者後四季 +0.32pp、<0.95 者 +0.45pp |
| 它能預測報酬嗎？ | **沒有證據**。≥0.95 者後 250 日報酬中位 +11.5%、<0.95 者 +13.8%，Spearman 僅 +0.041（448 個重疊觀測，獨立樣本遠少於此） |

之所以仍然採用：它修的是一個**內部矛盾**（同一組事實，有人讀過就看空、沒人讀過就看多），
不是在追求一個回測分數。誠實的說法是「讓規則與已實查論點對同一組事實給出一致的方向」，
**不是「這樣比較準」**。

#### 規則改版怎麼進帳本
`rule_1y.VERSION` 與資料雜湊**分開**追蹤（`roll_1y` 的狀態檔多存一個 `rule` 欄）。
規則改版會觸發一次重算，帳本記成「規則改版 X→Y」而非「財報已更新」——
後者會是一句假話：讀者以為有新財報，實際上是模型改了。
狀態檔沒有 `rule` 欄＝早於這個機制，視為舊版本並觸發一次遷移重算。

`gm_self_pct` 刻意**不進基礎雜湊**（`roll_1y.HASH_EXCLUDE`）：它是 `gross_margin`
歷史的確定性函數，而 `gross_margin` 與季別本來就在雜湊裡。把衍生欄位放進去，
唯一的效果是在新增欄位那天讓全部標的失配、被誤記成「財報已更新」（§5.4 警告過的事）。

### 5.3 檢查點（`src/checkpoints.py`）——一年期不能只押方向然後等
h=250 要 2027 年才結算，而且日頻預測在該尺度上重疊 249/250，
兩年資料每檔只有約 2 筆獨立觀測 —— 等一年拿到的不是統計，是軼事。

所以每一檔都必須寫檢查點：把論點拆成每月／每季可**自動對帳**的前提。

```python
{"claim": "毛利率 ≥60%（稀釋未超出管理層預告的 7pp）",
 "metric": "gross_margin", "op": ">=", "threshold": 0.60,
 "expects": "hold"}     # 或 "turn"
```

- **門檻要從管理層自己給的數字反推**：台積電法說會預告 N2 稀釋毛利率 3–4pp、
  海外廠 2–3pp → 67.7% − 7pp = 60.7%，所以門檻設 60%。
- **`expects` 兩種，寫判斷時必須擇一**：
  - `hold`（預設）＝賭它維持。**在基準日就被推翻是建構錯誤**，
    各 build 腳本的 `_audit_day_one()` 會擋下來不產檔。
  - `turn` ＝賭它翻過來（轉機型論點，例如「EPS 年增轉正」）。
    這種前提在寫下當天本來就是假的，那正是論點的內容；
    算成 broken 會讓面板第一天就顯示失效，把真警報淹掉。
    （`src/checkpoints.py` 已支援，但**截至 2026-09-19 沒有任何判斷檔用它** ——
    台股 2454 是典型的轉機型論點，下次重寫時應該標上。）
- **門檻一律相對現值，不能寫死**。成長率沒有上下界，統一寫 0 會同時犯兩種錯：
  對年增 +85% 的公司太鬆、對年增 −4% 的公司則是出生即死。現行分三段：
  `>20%` 要成長腰斬／`0–20%` 要轉負／已衰退則要再惡化 5pp。
- **判定**：過半前提被推翻 → 失效；有被推翻但未過半 → 動搖；
  全部成立 → 成立；全部尚未公告 → 待驗。
- **「成立」不代表準度信心高。** 它的用法是反向的：
  它不保證我對，但「失效」幾乎確定我錯了。

### 5.4 每日滾動（`src/roll_1y.py`）——三件事，不可混為一談
| 更新的東西 | 頻率 | 誰做 |
|---|---|---|
| 目標價、保守價、幅度（**重新定價**） | 每個交易日 | 機器 |
| P漲（**重新判斷**） | 台股每檔約 16 次／年、美股約 4.2 次／年 | 機器，僅限規則推導的標的 |
| 論點本身（**重寫**） | 被標記時 | 人，讀完資料再寫 |

**判準是「基礎雜湊」**：把 P漲 實際依據的輸入（已公告季報的季別與各項比率、
月營收月份與年增率、本益比取到小數一位）湊成一個雜湊。雜湊沒變就沿用昨天的
P漲，並在帳本裡標記 `repriced_only`。刻意**不納入**收盤價與 σ —— 那兩個歸重新定價管。

> ⚠️ **改雜湊的輸入等於把所有既有基準線作廢。** 曾把 `[code]` 改成 `[market, code]`
> 只是為了「看起來完整」，結果 38 檔台股在改版當天全部被判成「財報已更新」
> 而重算 P漲，實際上什麼都沒更新。要改就連帶寫一次遷移。

台美差四倍的原因很單純：**台灣有月營收，美國沒有**。
台股每月 10 日前公告月營收（12 次）＋季報（4 次）；美股只有季報。

### 5.5 一年期定價（`src/models/price_1y.py`）——只有一個原始量
2026-09-19 起，一年期的每一個數字都由 **P漲** 與 **σ** 兩個輸入、同一個對數常態導出：
```
Y ~ N(m, σ²)，m = σ·Φ⁻¹(P漲)              ← 令 P(Y>0) 恰等於 P漲
exp_ret（面板的期望值） = exp(m) − 1        ← 中位數
up（目標價）  = exp(m + σ·Φ⁻¹(1 − P漲/2)) − 1  ← 上漲情境的中位數
dn            = exp(m + σ·Φ⁻¹((1 − P漲)/2)) − 1  ← 下跌情境的中位數
q10／q90（保守價用 q10） = exp(m ∓ Z10·σ) − 1
mean_ret（僅記錄）= exp(m + σ²/2) − 1
σ = √250 × √(W_SHORT·vol_60² + (1 − W_SHORT)·vol_LR²)，W_SHORT = 0.6，
    vol_LR = 最近 1,000 日的日對數報酬標準差（LR_MIN, LR_MAX = 250, 1000；不足 250 日則不混合）
```
- **為什麼改**：舊版期望值走 §1.3 的兩點模型、區間走 σ=vol_60×√250 的對數常態，
  σ 到 0.6–1.0 時 σ²/2 的拖累達 0.2–0.5，中位數轉負。2026-09-19 實測台股 38 檔
  手寫 P漲 均值 0.552、自身區間隱含的 P漲 只有 0.406；南電 P漲 0.53 配區間
  [−83%, +140%]，隱含 P漲 0.33；82% 的 q10 < −50%。Brier 打的是手寫的 P漲，
  覆蓋率打的是那個區間 —— 兩個不同的預測。
- **為什麼 exp_ret 是中位數不是平均數**：對數常態的平均數含 exp(σ²/2)，σ=0.8 時是
  中位數的 1.38 倍。用平均數排序會變成純波動度排序（南亞科 +89%、台積電 +15%），
  而實證上高波動個股的一年平均報酬並不高。中位數 > 0 ⟺ P漲 > 0.5，看多／看空永遠與機率一致。
- **一年期沒有獨立的 skew**：對數常態的不對稱由 σ 決定。舊的 skew 對規則推導標的
  本來就是 P漲 的函數，拿掉不損失資訊，並消掉「P漲 50% 卻標看空」的矛盾。
- **σ 為什麼要混合**：vol_60 是 regime 讀數（2026-09-19 台股中位為自身長期的 1.22 倍）。
  16 檔五年以上台股、1,424 組觀測的 NNLS 擬合給 vol_60 權重 0.60（留一法 0.51–0.64），
  log 誤差 RMSE 由 0.321 降到 0.254。vol_60 在最高四分位時仍高估約一成，是已知殘餘。
  美股資料太短，沿用台股權重。
- **面板的年化波動在一年期顯示這個混合 σ**（帳本欄位 `sigma_annual`），不再顯示 vol_60。
- **區間的評分**：`settle.py` 除 `in_interval` 外另記 `interval_score`
  （寬度 + 10×越界量，α=0.2）與 `pinball_q10`／`pinball_q90`；`score.py` 並列
  `coverage_80` 與 `interval_score`，以及對氣候學基準的 `brier_skill`。
  只看覆蓋率會獎勵「寬到不可能不覆蓋」的區間，這正是舊版一年期區間的問題。
- **新的一年期 build／revise 腳本必須呼叫 `price_1y.price()`**，不要再複製
  `0.85 × vol × √250` 那段。`tests/test_price_1y.py` 會檢查帳本裡 2026-09-19 之後
  寫入的一年期紀錄是否與 P漲 一致。

### 5.6 新聞閘（`src/news_gate.py`）——財報看不到、但會改寫論點的事
```
收：併購分拆 · 財測調整 · 法規訴訟 · 經營層異動 · 重大投資
擋：目標價 · 分析師評等 · 股價回顧 · 法人買賣超 · 問句評論 · 推薦清單
```
- **刻意不管財報與月營收** —— 那兩者已經在基礎雜湊裡，再抓一次只是重複標記。
- **絕不自動改 P漲**：財報規則讀不了新聞，讓一則標題去改數字，
  等於用沒有依據的數字取代有依據的數字，而帳本會記成一次新判斷。
  它只標記「論點待重讀」。
- 排除優先於收錄，但四類強事件（併購／財測／法規／經營層）可越過排除 ——
  「台積電下修全年財測，預估營收成長由 30% 降至 25%」會因為「預估」二字被擋掉。
- 精準率約五成，刻意的：它的工作是把每天九百多則標題縮成 1–2 則給人讀。
- **去重**：以正規化標題建立長期記錄（`data/news_seen.json`，保存 120 天），
  只有第一次看到才算數。少了這步，一則併購會連續七天觸發重判。
- **氾濫保護**：一天內 >30% 且 ≥5 檔命中，當成比對異常或全市場事件，不逐檔標記。

---

## 6. 資料與 point-in-time 規則

| 內容 | 台股 | 美股 |
|---|---|---|
| 價量／籌碼 | FinMind（除權息自行還原） | yfinance（`auto_adjust=True`） |
| 季報 | FinMind，`src/features/fundamentals.py` | **SEC EDGAR XBRL**，`src/collect/sec_edgar.py`；取不到才退回 yfinance |
| 月營收 | FinMind，法定次月 10 日 | **不存在** |
| 新聞 | Google News RSS（必加財經關鍵字） | 同左 |

**可用日一律用法定公告／申報上限，不用期末日。**
拿期末日當可用日是最典型的前視偏差，而且**不會讓回測失敗、只會讓回測變漂亮**。

- 台股（證交法 §36）：Q1→5/15、Q2→8/14、Q3→11/14、Q4→次年 3/31
- 美股：10-Q 季末後 40 天、10-K 年度結束後 60 天；外國發行人 45／90

### 6.1 美股改用真實申報日（2026-09-19 起）

上面那組法定上限是**法律容許的最晚一天**，不是公司實際公布的那一天。
實測 49 檔美股 2015 年後的真實申報落後中位數是 **32 天**，比 10-Q 的 40 天早
8 天 —— 舊做法讓我們比真實世界晚八天才知道前提翻掉。

SEC 的 XBRL companyfacts 每一筆 fact 自帶 `filed`（EDGAR 收件日），不必推估。
一併解決的是歷史深度：yfinance 一次只給 5–7 季，SEC 給到 2007 年
（49 檔的累積期別中位數 **74 季**），`gm_self_pct` 需要的 8 季因此一次到位。

| 規則 | 做法 |
|---|---|
| 可用日 | `filed_first`（該季各欄位首次公布日的最小值） |
| 退回法定上限 | 沒有 SEC 資料，或申報日離期末超過 `MAX_FILED_LAG = 120` 天 |
| 退回要留痕 | 逐列記在 `avail_basis`（`SEC申報日`／`法定上限`），實測 92.5% 走 SEC |
| 數字版本 | 一律取 `filed` 最小的那一版（**原始申報值，不取重編值**） |
| 只採信定期報表 | 10-K／10-Q／20-F／40-F 及其更正版 |
| 同一檔不混用兩個來源 | 期末日與銀行營收定義兩邊都不一樣 |

**為什麼要擋非定期報表**：CAT 的 FY2021 淨利在 `NetIncomeLoss` 底下只有一筆，
來源是 2026-04-30 的 DEF 14A（委託書薪酬對照表要揭露前五年淨利）。
照單全收的話那一季的可用日會變成四年後，整季在回測裡消失。

**為什麼標籤不能補洞式合併**：AXP 2017 年起的
`RevenueFromContractWithCustomerExcludingAssessedTax` 對金融股只算合約收入
（單季約 60 億），2016 以前的 `Revenues` 是總收入淨額（約 80 億）。
接起來會讓 2017Q1 的 `rev_yoy` 憑空出現 −25%，而公司什麼事都沒發生。
所以規則是**覆蓋最多的標籤當主序列，其餘只有在重疊期別上數值差 <1% 時才併入**；
沒有重疊期別就無從驗證，一律不併。寧可少一段歷史，不要一段換過定義的歷史。

**換來源會動到 `roll_1y` 的基礎雜湊。** 雜湊的輸入含「最新已公告季報的季別」
（§5.4），而 SEC 用的是真正的財報週期末（AAPL 2026-06-27），yfinance 用月底
（2026-06-30）—— 實測 49/50 檔的雜湊會變，而其中絕大多數的財務數字一模一樣。
切換當下 `data/roll_1y_state.json` 的 US 區段是空的，所以走的是
「首次滾動，沿用原判斷」那條，沒有任何標的被誤記成財報已更新。
**但反向切回 yfinance 會讓同樣的 49 檔全部失配**，屆時要照 §7 第 3 條寫遷移，
不能直接改。（AMZN 是唯一一檔換來源後季別真的往前走的：yfinance 的 2026Q2
只有 EPS 一欄、沒有營收而被丟棄，SEC 那一季是完整的。）

**不涵蓋 IFRS 申報人。** TSM 的 companyfacts 只有 `ifrs-full`，us-gaap 概念是
0 個，它仍走 yfinance 並標記 `法定上限`。造一份假的對照比沒有對照更糟。

**`data/raw/sec_fund/` 進版控**，理由與 `us_fund/` 同一條：不進版控的話
GitHub Actions 每次從空的開始，`roll_1y` 的基礎雜湊與本機不同，
於是每次排程都把全部美股判成「財報已更新」而重算 P漲。

SEC 的 fair access 規定要求 User-Agent 帶聯絡方式，否則 403。
走環境變數 `SEC_UA`，**不寫進 repo**；GitHub Actions 設成 secret。

**除權息必須還原，且價格與股數兩邊都要**：價格乘 factor、股數除以 factor。
只還原價格的話，國巨 1:4 分割會讓 `margin_chg_5` 衝到 +2.86（全庫 99.89 百分位），
被模型讀成「散戶瘋狂加槓桿」。

`data/raw/us_fund/` **進版控**，因為它不是快取，是逐日累積的 point-in-time 檔案庫
（yfinance 一次只給 5–7 季）。`data/roll_1y_state.json` 同理，它是重新定價與
重新判斷的分界基準。

---

## 7. 方法變更的程序

1. **改判斷方法**（錨點、權重、門檻）→ 寫進當天的 build 檔頂端 ＋ `LESSONS.md`，
   說明舊依據為何不再成立。錨點尤其重要，它是整批判斷的共同平移項。
2. **改基準線** → 原則上不改（見 §2）。非改不可就當新模型另開名字。
3. **改雜湊輸入／特徵定義** → 檢查既有基準線是否全部失效，必要時寫遷移。
4. **改共用數學**（`quantiles.py` 的 z、`0.85` 係數）→ 影響所有歷史比較，
   要在 `data/revisions.jsonl` 留痕，並確認 `_material()` 抓得到這種變更。
5. **改完一律跑** `./.venv/bin/python -m pytest tests/ -q`（全套）。

**這份文件自己有測試守著。** `tests/test_method_doc.py` 會把上面列出的每一個
常數與程式碼對照，對不上就失敗。所以改了程式裡的常數而忘了改這份文件，
CI 會擋下來 —— 文件與程式碼不同步是最常見的失效方式，而它平常不會出聲。

---

## 8. 這份文件沒有涵蓋的

- 每日執行流程 → `DAILY.md`
- 已知錯誤型態清冊 → `LESSONS.md`（512 行，每一條都真的發生過）
- 目前的狀態與待辦 → `HANDOFF.md`
- 回測結論與四道防線 → `README.md`
- **為什麼回測結論目前不可用（生存者偏差）** → `README.md` 最上方，必讀
