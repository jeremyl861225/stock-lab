# ITERATE — 這套系統怎麼自我迭代

**目的：讓系統每週自己量出哪裡錯、把證據擺好、提出改法、驗證改法、決定要不要採用，
而且整個過程不能自我欺騙。** 程式在 `src/iterate.py`，每週指令見 `WEEKLY.md`。

這份文件回答四個問題：什麼叫「有進步」、迴圈長什麼樣、候選模型怎麼晉升、
以及「回測準確率 >90%」這個要求怎麼處理。

---

## 0. 先講結論：什麼叫「有進步」

只有一種證據算數：**前瞻結算**（`data/settlements.jsonl`）上，**分市場、逐日群集、依重疊折減後**的 t。

| 尺度 | 用途 | 不能拿來做什麼 |
|---|---|---|
| 前瞻結算（折減後 t ≥ 2、有效天數 ≥ 20） | 宣稱某條線／某個改法有效 | 有效天數 < 4 時連 p 值都不出（`score.py`） |
| 誠實回測（PIT、walk-forward） | **排除**：外洩、明顯劣於猜漲、調參無路 | 宣稱有效（研究者自由度消不掉，且樣本只有一段多頭） |
| 神諭天花板（`leak_canary.py`） | 判斷一個回測數字可不可信 | 當成目標 |

有效天數 = 名目天數 ÷ 重疊倍數（日頻預測、h 日期間，相鄰重疊 (h−1)/h）。
h=20 要 20 個有效天數，等於約 400 個日頻批次、將近兩年。**這不是缺陷，是這個問題本身的難度。**
所以迴圈的重心放在累積證據最快的那條線：**選股判斷**（一天 50 個決定），
而不是市場判斷（一天 1 個決定，證據累積速度差 50 倍，`METHOD.md` §4.3）。

---

## 1. 「歷史回測準確率 >90%」為什麼是外洩警報，不是目標

2026-09-24 用凍結快照量出來的（`data/backtest/leak_canary.json`，`./.venv/bin/python src/leak_canary.py` 可重跑）：

| 資訊集（PIT 成分股，方向準確率） | TW 5 日 | TW 20 日 | US 5 日 | US 20 日 |
|---|---|---|---|---|
| 無條件猜漲（基準線） | 56.1% | 63.8% | 51.5% | 51.6% |
| 誠實 stat_logit（walk-forward） | 51.9% | 59.3% | 51.4% | 50.8% |
| **完美市場擇時**（每天知道市場正負） | 66.3% | 68.1% | 60.5% | 60.2% |
| **完美相對強弱**（知道每檔去均值報酬正負，減均值） | 75.2% | 73.5% | 87.3% | 85.8% |
| 同上，減中位數 | 81.8% | 81.1% | 88.6% | 88.8% |
| 標籤外洩：sign(未來報酬)＋N(0,1)（訊噪比 1） | 84.7% | 84.2% | 83.5% | 82.7% |
| 把未來報酬本身當特徵 | 99.6% | 99.7% | 99.6% | 99.5% |

三件事：
1. **連「每天都知道市場漲跌」的神諭都只有 60–68%。** 90% 高於「知道每一檔相對強弱正負號」的天花板。
2. 要到 90%，只有直接知道未來報酬的幅度，或注入訊噪比約 1.3 以上的標籤外洩；
   誠實特徵的 rank IC 只有 0.00–0.02，連訊噪比 0.2 的外洩（IC 0.10–0.22）都不到。
3. 54 組超參數、精簡特徵、Platt 校準、相對報酬標籤全部 walk-forward 試過（2026-09-24 審查），
   沒有任何一組贏過猜漲；看起來贏的都是「往基本率收縮」。

所以這個要求被轉成一條**守門規則**（`leak_canary.guard`，`tests/test_leak_guard.py`）：
任何模型在回測裡的方向準確率超過「完美相對強弱（中位數版）」、或 |rank IC| > 0.3，
CI 直接失敗並標成外洩嫌疑。它是煙霧偵測器不是定理（同時握有部分幅度資訊的誠實模型理論上可以越線），
但它抓得到「把答案混進特徵」那種，而那正是回測數字突然變漂亮時最常見的原因。

可執行、可驗證的目標改成：
- 選股線：前瞻 rank IC 折減後 t ≥ 2（有效天數 ≥ 20）；
- 市場線：手寫錨點對兩條機械錨（過去 250 日上漲率、及其向 0.5 收縮一半）的 Brier 不落後；
- 區間：名目 80% 覆蓋率在 8 個獨立日後落在 [0.70, 0.90]，且上／下方越界比率與錨點偏差同號；
- 否證條件：每一筆逐檔、機械可判定，被觸發率成為可量化指標。

---

## 2. 迴圈

```
每日  daily.py prepare ─▶ 判斷 ─▶ verify_judgment ─▶ finalize ─▶ settle
                                        ▲                          │
每週  iterate.py ◀─────── 結算累積 ◀─────┘                          │
        │  A 成績單（分市場、折減 t）                                │
        │  B 市場判斷 vs 選股判斷                                    │
        │  C 錯最大的 N 檔 ＋ 當時論點 ＋ 否證條件機械判定  ─▶ 人做三分法 ─▶ LESSONS.md
        │  D 區間覆蓋與越界方向                                          │
        │  E 查核器警告數                                     新型態 ─▶ verify_judgment 新規則
        │  F 外洩守門                                                    │
        └▶ G 建議  ─▶ 人決定 ─▶ decisions 寫進 data/iterations.jsonl（append-only）
                                  │
                       改判斷流程（DAILY.md 規則、錨點、分散度）── 下週 A/B 看有沒有動
                       改模型 ─▶ 候選模型 ─▶ gate ─▶ 以新名字影子上線 ─▶ 前瞻 20 個有效天 ─▶ 才上面板
```

### A 成績單（`score.summary()`）
- `by_horizon_market[h][market]`：分市場算基本率、配對差、p 值。混池的氣候學被市場間變異墊高，技能分數會系統性灌高。
- `accuracy` 用 sign(期望值) 判方向（決策）；`accuracy_by_prob` 用 P漲>0.5（機率）。
  兩者差很多＝方向與機率是兩個模型（`momentum` 的「準確率」其實是 always_up：99.9% 的方向都是 +1）。
- `brier_skill` 對**長期基本率**（`config.LONGRUN_BASE_RATE`，事前常數）；對當批事後上漲比率的那個（`brier_skill_insample`）
  任何常數預測必為負，只供對照。
- 中性判斷（P漲 0.5、期望值 0）記為棄權，不進命中率分母；Brier 照算（棄權不能逃掉機率的罰分）。
- p 值：逐日配對差的群集 t，再除以 √(h/取樣間隔)；有效天數 < 4 → `None`，不是 0.5。

### B 判斷拆解（`attribution.py`）
- 市場判斷＝錨點 − 當日實際上漲比率。沒寫錨點的批次以該批 p 均值當**隱含錨**並標 `implied`（首批實際押的是 0.50，不是文件的 0.53）。
- h5 的錨點先依 √t 換到 5 日尺度（判斷檔的 anchor 是 p20 的錨）。
- 選股判斷全部去均值；`sel_hit` 走名次版（對均值去均值在右偏日會把多數檔記成輸給市場）。

### C 錯誤歸因包
`iterate.py` 列出 claude 錯最大的 12 檔，附當時的 thesis 與 falsifier，**由人**填 `classify_as`：

| 類型 | 判準 | 處置 |
|---|---|---|
| 事實錯 | 引用的數字或事件與 panel／新聞不符 | LESSONS ＋ 修管線；`verify_judgment` 補規則 |
| 推論錯 | 事實對，從事實到結論站不住（例：把「已漲多」當 5 日空方理由，而反轉在這段樣本 IC 為負） | LESSONS ＋ `verify_judgment` 補規則（如 `check_reversal`） |
| 推論對但市場不理會 | 事實與邏輯都對，價格沒反應 | **不是錯誤**，是市場效率；連續出現才考慮放棄這個角度 |

否證條件機械判定：`falsifier_check` 解析「N 日內漲／跌逾 X%」對 panel 判 hit／not_hit；
判不了（unparseable）的比率本身就是指標 —— 首批 50 檔共用一句、三條沒有一條涵蓋錯最大的 7 檔。
**規則：否證條件逐檔寫、含該檔自己的欄位、數字門檻與期間。** `verify_judgment.check_structure` 會擋整批共用。

### D 區間
覆蓋率之外看上／下方越界比率：首批 claude 12 檔越界全在上方，那是市場判斷偏空的另一個讀數。
同日相關讓一批只值約 1/6 個獨立批；SE ≤ 0.05 要 8 個獨立日（約 2 個月）。

### E 查核器要跟著 LESSONS 長
每一種被人工歸因抓到、而查核器 0 命中的型態，都要變成 `verify_judgment.py` 的一條規則。
2026-09-24 加了四類：結構（否證共用／事實推論不分欄／文字無方向但 p≠錨）、分散度、反轉。
噪音太多的警報等於沒有警報：新規則先看它對最近三批各發幾項，誤報就收窄。

### F 外洩守門
`leak_canary.guard` 對 `data/backtest/results.parquet`；`tests/test_leak_guard.py` 在 CI 跑。
資料層另有 `tests/test_panel_availability.py`（月營收 10 日前不得變動、法人與融資的同日對位）。

### G 建議只建議
`iterate.py` 依規則產生建議（有效天數不足→只做歸因；選股 t ≥ 2→可調分散度；否證共用→逐檔寫……），
人決定後把 `decisions` 寫進 `data/iterations.jsonl`。**每個決定要寫「預期下週哪個數字會動」**，
下週的 A／B 對照它。動了不算證明，沒動要問為什麼。

---

## 3. 分散度規則（選股線唯一可調的旋鈕）

二元常態下讓 log-score 最大的 P漲 橫斷面標準差 ≈ 0.4 × IC（IC 0.03→0.012、0.07→0.027、0.10→0.040）。
walk-forward 實測（TW PIT）：×0.5–1 最好、×2 起 Brier 明顯變差。
- IC_hat 取前瞻 `sel_rank_ic`（有效天數 ≥ 20），沒有時預設 0.04 → 目標 sd 0.015。
- `verify_judgment.check_dispersion` 在 [0.5, 2] 倍以外警告。**一次最多調 ×1.5，下一輪再看。**
- 不要以「證據混雜就收斂到錨點」把整批壓到 0.008：那等於放棄選股線的計分。

## 4. 錨點（市場線）

不改手寫錨點 —— 它是要被檢驗的 regime 判斷。但每批同時記三個錨：手寫、規則 b（該市場過去 250 日 h 日上漲率）、
規則 c（b 向 0.5 收縮一半），`attribution` 各算市場判斷 Brier。要 15–36 個獨立 20 日視窗（1.2–2.9 年）
才分得出高下；**在那之前不換規則**。單一多頭期間任何高於 0.55 的常數都會「贏」，那不是規則的能力
（2026-09-24 審查 CJ-05；METHOD §4.3 作廢舊錨 0.58–0.68 的理由正是同一件事）。

## 5. 候選模型晉升關卡（`iterate.py --gate`）

候選模型交一份與 `backtest.results` 同 schema 的 walk-forward 結果（PIT 列）。每個 (市場, 期別) 各判：
1. 有效天數 ≥ 20；
2. 不冒煙（守門）；
3. 對 always_up 逐日配對的 Brier 差折減後 t ≤ −2，或配對準確率差 t ≥ 2；
4. rank IC 折減後 t ≥ 2（要有選股資訊，不是只把基本率校準得更好 —— Platt 校準與精簡特徵集都死在這一條）。

通過只代表「可以以**新名字**影子上線」（`src/models/` 新模組、`predict.py` 註冊、獨立 VERSION；基準線與既有模型凍結）。
影子模型每天照寫帳本、照結算，累積 20 個有效天數且前瞻 t ≥ 2 才上面板。
帳本 pid 不含版本、只准 append，所以影子模型的成績不可能被事後洗掉。

## 6. 研究者自由度怎麼管

- 每次嘗試（不論成敗）都 append 到 `data/iterations.jsonl` 的 `decisions`／`research/iterations/`。
  試了 20 個改法總有一個看起來顯著：把 20 個都記下來，讀者才知道那個「顯著」是 1/20。
- 特徵選擇、超參數只能用評分窗**之前**的資料選，選完凍結；每日重選＝過擬合。
- 回測改動（新特徵定義、新可用日）一律新版本號＋`revisions.jsonl` 留痕（`METHOD.md` §7）。

## 7. 已知的極限

- 樣本只有一段多頭（2024-08 起）；下跌月的證據只有 4–6 個月，不能引用。
- Claude 判斷無法回測；它的一切證據從 2026-09-16 起算。
- 一年期要 2027-09 才有第一批結算；中途只有檢查點對帳。
