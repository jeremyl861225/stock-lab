.venv/
__pycache__/
*.pyc
logs/
.DS_Store

# 原始快取一律不進版控（finmind 92MB、news 1.8MB），
# 唯獨美股季報例外 —— 它不是快取，是**逐日累積的 point-in-time 檔案庫**。
# yfinance 一次只給最近 5–7 季，這份檔案把每次抓到的期別併起來保存；
# 不進版控的話，GitHub Actions 每次都從空的開始，只看得到 7 季，
# 算出來的基礎雜湊會與本機不同，於是每一次排程滾動都把全部美股
# 判成「財報已更新」而重算 P漲 —— 實際上什麼都沒更新。200KB，值得。
data/raw/*
!data/raw/us_fund/
# SEC companyfacts 蒸餾後的季別檔（49 檔、1.6MB）。進版控的理由與 us_fund
# 同一條：不進的話 GitHub Actions 每次從空的開始，roll_1y 的基礎雜湊與本機
# 不同，每次排程都把全部美股判成「財報已更新」而重算 P漲。
!data/raw/sec_fund/

data/features/
data/backtest/*.parquet
