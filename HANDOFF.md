# HANDOFF

**先讀 `METHOD.md`** —— 那是「這套系統怎麼做出每一筆預測」的完整規格，
自足、不依賴任何對話記憶。這一份只講**現在的狀態與待辦**。

| 文件 | 內容 |
|---|---|
| `METHOD.md` | 每一種預測的完整方法與常數出處（有測試守著同步） |
| `DAILY.md` | 每日執行 SOP（含一年期論點的維護） |
| `LESSONS.md` | 已發生過的錯誤型態清冊，做判斷前必讀 |
| `README.md` | 系統目的、四道防線、回測結論與**為何目前不可用** |

---

## 2026-09-24：五面向審查 → Opus 反駁 → 修（先讀這段）

五個審查員各自實跑程式、每條發現兩位反駁者重跑數字（53 個代理），確認 18 條、反駁 6 條。
方法文件 `ITERATE.md`（自我迭代怎麼做、什麼叫有進步、**為何回測 >90% 是外洩警報**）。

已修（每項有測試）：
- 計分定義四處：中性判斷記棄權（`settle`／`score`／`accuracy`）；並列 `accuracy_by_prob`；
  `brier_skill` 對長期基本率（`config.LONGRUN_BASE_RATE`）；p 值改逐日群集 t 折減、有效天數 < 4 不出；
  `by_horizon_market` 分市場。`tests/test_score_math.py`、`tests/test_settle_math.py`。
- `accuracy.calib_slope` 拆成市場層／選股層 MZ（單日 −0.746 是雜訊，CI [−6.6, +4.6]）。
- `attribution`：沒錨點的批次用隱含錨（首批實際押 0.50 不是 0.53）、h5 錨點依 √t 換算、sel_hit 走名次版。
- 外洩守門 `src/leak_canary.py` ＋ `tests/test_leak_guard.py`；欄位可用日 `tests/test_panel_availability.py`。
- 查核器新增結構／分散度／反轉三類（對 9/23 的判斷檔立刻命中 4 項）。
- 每週迭代 `src/iterate.py` → `data/iterations.jsonl`（append-only）＋ `research/iterations/`。
- 面板：市場判讀改 `<details>` 收合、依期別各顯示自己那份、美股用自己的 as_of；
  卡片價位以判斷日收盤為基準（美股 21 檔曾偏到 5%）；`charts.json` 改 network-first。
- Actions：`daily.py` 的 PY 退回 `sys.executable`（排程自 9/19 起每天 FileNotFoundError，6 次 0 提交）；
  cron 改台北 23:00；提交後斷言作者是 bot。

審查後**刻意不做**的（反駁成立，證據在 `research/iterations/` 與 LESSONS）：
Platt 校準新模型（比基本率還差的氣候線）、精簡特徵集（退化成猜漲）、相對報酬標籤、
改錨點規則（單一多頭）、改 √t（分不出對錯）。

**排程跑通後的新風險**：runner 會在 23:00 先把當日基準線與統計模型寫進 append-only 帳本
（pid 先寫者優先），本機隔天早上只補判斷；runner 的 raw 資料不進版控、每次全量重抓，
panel 可能與本機不同。第一週要每天對一次 `git log --author=stock-lab-bot` 與 predictions 的 run_id。

## 狀態（2026-09-19）

- 基準日 **2026-09-18 收盤**。`predictions.jsonl` 5,858 筆、5,572 個邏輯鍵。
- repo 已是 public `jeremyl861225/stock-lab`，Pages 上線，
  GitHub Actions 每交易日台北 18:00 跑 `daily.py prepare` ＋ ranking ＋ panel。
- 全套測試通過（`./.venv/bin/python -m pytest tests/ -q`）。

| 市場 | 5／20 日 | 一年期 |
|---|---|---|
| 台股 | 50 檔 | 38 檔、110 條檢查點 |
| 美股 | 53 檔 | 39 檔、117 條檢查點（9 檔已實查前瞻計畫） |

- 一年期每日滾動中（`src/roll_1y.py`），新聞閘接在同一步（`src/news_gate.py`）。
- **尚無任何預測到期結算**（`data/settlements.jsonl` 還不存在）。
  第一批 5 日約 2026-09-23、20 日約 2026-10-15。**一年期要到 2027 年 9 月。**
  在那之前，這套系統的所有「成績」都還不存在 —— 檢查點是唯一的中途回饋。

---

## 生存者偏差：兩層已修，剩下的是樣本期間

- **特徵本體帶前視資訊** —— 已移除。`mkt_ret_5`／`mkt_ret_20`／`xs_ret_20`
  早已不在 `FEATURE_COLS`（當日組內標準差恆為 0，對橫斷面排序零貢獻）。
- **評分集合用「今天的市值前 50」** —— **2026-09-19 已修**。台股回測改用
  `config/universe/` 的 26 份每月快照逐日還原（`universe.load(as_of)`）。
  量出來的代價：**20 日上漲基本率 68.4% → 61.7%（−6.7pp）**，
  而 `momentum` 的 IC 調整 t 由 2.14 掉到 0.57 ——
  舊 README 唯一「通過檢驗」的數字整個由樣本選擇造出來。
- **統計模型的訓練集仍未做 PIT 限制** —— 未修。評分是 PIT 的，訓練仍用
  panel 的全部代號。影響小於評分集合，但不是零。
- **美股沒有歷史成分股快照** —— **2026-09-19 已修**。
  `src/collect/pit_universe_us.py` 用 SEC 申報股數 × 當時未還原收盤價重建
  26 份每月快照（2024-08 ~ 2026-09）。重建的最新前 50 與即時 universe
  **50/50 吻合**、排名 Spearman **0.9972**。代價量出來是
  **20 日基本率 58.3% → 55.3%（−3.0pp）**、5 日 −1.5pp。
  **但它比台股弱一級**：台股抄 0050 的實際籃子，美股是自己重算排序，
  候選池仍是今天的 80 檔清單。詳見 `METHOD.md` §6.2。
- **樣本期間只有一段多頭** —— 修不掉。2024-08～2026-09 全程是 AI 硬體循環上行段。

---

## 待辦（依嚴重度）

### 一、會讓判斷失真的
1. ~~**規則模型缺「週期位置」修正項**~~ —— **2026-09-19 已修**（`rule_1y` 1.1.0）。
   加了 `gm_self_pct`（當期毛利率相對自身歷史的百分位），
   成長與估值類訊號在自身高點衰減到 0 並扣 0.05。
   華邦電 0.621→0.515、南亞科 0.631→0.527。見 `METHOD.md` §5.2。
   **美股已生效**（2026-09-19）：改走 SEC EDGAR 後累積期別中位數 74 季，
   `gm_self_pct` 覆蓋率 62.3%（原本 0%，因為 yfinance 只給 5–7 季）。
2. **台股的一次性利益未處理**。美股已加 `nonop_heavy`（|淨利| > 2×|營業利益|
   就改用營業利益本益比，實測 GOOGL 帳面 17.4 → 實際 36.9）。
   FinMind 的 EPS 同樣含業外，台股尚未做同一道檢查。
3. **一年期錨點可能偏低**。`ANCHOR = 0.55` 的依據是「個股長期報酬中位數低於指數」，
   但 13 年資料顯示這批大型股的一年勝率中位數是 66%。要用 PIT universe 重驗後再改。
4. ~~**`_binom_p` 假設 i.i.d.**~~ —— **2026-09-24 已修**：逐日群集 t 折減、有效天數 < 4 不出 p。

### 二、會讓稽核失效的
5. `feature_hash` 對 claude 判斷是空的，且 panel 在 .gitignore 內、每日重建會回溯改寫，
   所以無法事後驗證「當時看到的特徵長什麼樣」。2026-09-24 量化：stat_logit 的 hash 對快照重算
   5 個市場日只有 2 天對得上；美股起點滾動已由 ed08514「只增不減」修掉一部分，台股 0/50 原因未定。
   對準確度影響 ≤0.8pp，是稽核可信度問題。
6. 停牌時 horizon 失真（2327 停止買賣期間，「5 日預測」用了 12 個市場日結算）。
7. 1/n 分割判斷：n≥6 後帶狀重疊，−72%~−90.5% 的跌幅都會被判成分割；
   f>1 側（減資／併股）永遠判不到。

### 三、資料源
8. ~~**美股季報走 yfinance**，只給 5–7 季、可用日只能推估~~ ——
   **2026-09-19 已修**。改走 SEC EDGAR XBRL（`src/collect/sec_edgar.py`），
   49/50 檔有資料（TSM 用 IFRS，仍走 yfinance）。方法見 `METHOD.md` §6.1。
   後半段的美股 PIT universe 也一併做完了（`collect/pit_universe_us.py`）。
9. 金融股不在一年期判斷內（台股金控 8%、美股純金融 8.6% 權重）。
   損益表沒有毛利率與營業利益，要另建 NIM／信用成本／淨值成長那套特徵。
10. ETF（VOO／QQQ／BTCO，約 4.6%）不在一年期判斷內，沒有公司財報可判斷。

### 四、其他
11. `expects="turn"` 已在 `checkpoints.py` 實作但尚無判斷使用。
    台股 2454 是典型的轉機型論點，下次重寫時應標上。
12. **否證條件逐檔寫**（2026-09-24）：目前每個 5／20 日檔仍是整批共用一句；`iterate.py` 的機械判定對首批 49 筆全部「判不了」。
    下一份 build 檔起改為 (code, p20, skew, conviction, why, falsifier) 六元組。
13. **零報酬列**（5 日 1.4%）被 settle 記成下跌；停牌股用位置數 horizon（2327 曾用 12 個市場日）。低優先。
14. **一年期規則的 why** 在 `gm_self_pct` 為 NaN 時應明寫「週期位置修正不適用」（金融與 XOM／LIN 永遠不生效）。

### 五、2026-09-19 這一輪做完的事（詳見 `data/revisions.jsonl` 最後六筆）
1. 一年期定價改為單一對數常態（`models/price_1y.py`）—— 原本 P漲、期望值、
   區間來自三套互不相容的算法，手寫 P漲 0.552 對上區間隱含的 0.406。
2. 統計模型分市場訓練 —— 美股 7 個特徵 100% 缺值曾被填成台股中位數。
3. 成績拆成市場判斷與選股判斷（`src/attribution.py`），錨點進帳本。
4. 新增 `mech_core` 基準線 —— 判斷有三分之二可被「跌深×營收強」複製（Spearman +0.736），
   必須贏過它才算有增量。
5. 事件日曆（`src/events.py`）—— 視窗內有沒有財報／月營收／除權息。
6. 一年期規則的週期位置修正（本頁第 1 項）。
7. 回測改用 PIT 成分股且不再把台美擠進同一個橫斷面 ——
   這一項量出了生存者偏差的代價：20 日基本率 −6.7pp、momentum 的 t 由 2.14 → 0.57。

8. 美股季報改走 SEC EDGAR（`src/collect/sec_edgar.py`）——
   可用日從「法定上限推估」變成真實申報日（2015 年後 92.5% 走 SEC，
   落後中位數 32 天 vs 法定 40 天），歷史深度 5–7 季 → 74 季。
   過程中抓到三個會靜默失真的東西，各有測試釘住：
   委託書（DEF 14A）的薪酬對照表被當成財報首次公布（CAT 差四年）、
   標籤補洞式合併讓 AXP 的 rev_yoy 憑空多一段 −25%、
   同一期末有多個起始日時靠 set 走訪順序決定取哪一筆（GS 每次跑結果不同）。

9. 美股 PIT 成分股（`src/collect/pit_universe_us.py`）—— 26 份每月快照，
   偏差量出來是 20 日基本率 −3.0pp。過程中修掉四個不會報錯的錯：
   分割落在兩次申報之間（NVDA 少算十倍、掉出前 50）、多重股權的股數只報一個
   級別（BRK-B／MA／V 三檔兆元公司一起掉出榜外）、ADR 的股數是當地普通股
   （TSM 算成 4.76 倍）、以及**對帳只查自己選中的人**所以完全沒發現前兩項。
   順帶抓到 `collect/us.py` 的 `load(as_of)` 是假的 —— 簽名收下 as_of
   卻永遠回傳最新 universe，台股那支一直是真的，兩邊介面一樣、行為不同。

**尚未驗證的部分**：以上全部沒有任何結算資料佐證，第一批 5 日預測約 2026-09-23 到期。
在那之前，這些改動的理由都是「內部一致性」與「已知偏差的消除」，不是「實測比較準」。
12. `momentum` 基準線的分位數仍用常態而非對數常態。**刻意不修** ——
    基準線一旦上線就凍結，改了之前的比較就不可比（見 `METHOD.md` §2）。

---

## 新增的環境需求（2026-09-19）

SEC 的 fair access 規定要求 User-Agent 帶聯絡方式，否則一律 403：

```bash
export SEC_UA='stock-lab research <你的信箱>'
```

**GitHub Actions 要另外設 secret `SEC_UA`**，`daily.yml` 已經在傳了。
沒設的話 `collect/sec_edgar.py` 會拋錯 —— 但它在 `daily.py` 裡是
`allow_fail=True`，所以管線不會中斷，只會安靜退回 yfinance＋法定上限。
**這代表「忘了設 secret」不會有人告訴你**，只會讓 `avail_basis` 整欄
變成「法定上限」。要查就看 `./.venv/bin/python src/features/us_fundamentals.py`
第二行印出來的來源分布。

---

## 兩個必須知道的操作陷阱

1. **`daily.py finalize` 的自動提交白名單不含 `src/`。**
   改過程式碼要自己補一次 commit，並用 `git show --stat HEAD` 自驗 ——
   曾發生 `git add` 整批失敗、commit 卻照樣成功並印出「已推送」。
2. **這個 repo 常有另一個 session 同時在改。** 動 `src/` 之前先 `git fetch`，
   確認沒有剛進來的 commit 動過同一個檔（2026-09-19 差點蓋掉 `roll_1y.py`
   的 `stance` 修正）。
