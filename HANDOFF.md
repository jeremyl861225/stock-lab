# HANDOFF

## 狀態：可運作，但**回測結論暫不可用**（生存者偏差未修）

- 基準日 **2026-09-16 收盤**。predictions.jsonl 1,700 筆、600 組邏輯鍵。
- Claude 判斷 v2（50 檔 × 5日／20日），推理鏈在 data/reasoning.jsonl。
- 第一批成績：5 日約 **2026-09-23**、20 日約 **2026-10-15** 可結算。

## 兩輪獨立審核的結果（2026-09-17）

用 subagent 審了「判斷內容」與「資料管線」兩塊，各抓到一個致命問題。

### 判斷內容（已修，v1 → v2）
**判斷日錯置**：市場判讀用 9/15 的恐慌解釋 9/16 的反彈。基準日當天實際是
等權 +1.21%、39/50 檔上漲、台積電完成填息、費半衝高 1.7%。

量化修正：
| | v1 | v2 |
|---|---|---|
| 金融股 p 與營收相關 | −0.234（方向相反） | **+0.441** |
| 純幅度與波動度相關 | −0.756 | **+0.041** |
| 幅度最寬/最窄倍數 | 2.85 | **1.00**（恆定 0.8σ） |
| 5 日漂移 | EV₂₀/2 | **EV₂₀/4** |

另修 9 項事實錯誤，含「欣興跌停是被搜索／洗產地司法事件而非需求」
（營收其實 +56%）、「台灣大融資 +1151% 是基數效應」（98→1,226 張）。

### 資料管線（已修 5 項）
1. **恆真測試**：`test_truncation_invariance` 原比較 `f(trunc(X))` 與
   `f(trunc(trunc(X)))`，數學上不可能失敗，卻被 README 列為第一道防線。已改寫。
2. **股數欄位未還原**：volume／margin_bal／short_bal／foreign／trust／dealer
   都沒隨除權息調整。國巨 1:4 分割後 margin_chg_5 達 +2.86（99.89 百分位），
   緯穎配股正在汙染 live 特徵（+1.600 → 修正後 **−0.128**，方向完全相反）。
3. **`_pid` 含 model_version**：等於替同一邏輯鍵重開槽，已產生 600 組重複。已移除。
4. **backtest 跑不起來**：baseline 簽章改了沒同步，第一個日期就拋 TypeError。
   README 的頭條數字因此是舊版程式的孤兒檔。
5. **score 無 JSON 防護**：一行壞掉整份成績單掛掉。

## ⚠️ 未修的最嚴重問題：生存者偏差

整份 2 年 panel 使用**「今天」的市值前 50**。這 50 檔兩年等權 **+272%、49/50 上漲**。

後果有兩層：
- always_up 的基本率有相當部分是「因為這批股票後來漲了才被選進 universe」造出來的
- 更嚴重：`mkt_ret_5` / `mkt_ret_20` / `xs_ret_20` 三個特徵本身就是這批倖存者的
  橫斷面統計，**特徵本體帶有前視資訊**

**在這件事修好之前，所有「模型 vs 基準線」的比較與 RankIC 都不能當證據。**

修法：每日存 universe 快照、`universe.load(as_of)` 全面帶入（目前六個呼叫點都沒傳）、
panel 保留所有曾入選過的代號。需要歷史各時點的全市場市值資料。

## 其他待修（依嚴重度）

1. `_binom_p` 假設 i.i.d.，Monte Carlo 實測名目 α=0.05、實際拒絕率 0.299（寬鬆 6 倍）。
   crosssec 有做重疊調整，score 沒有 —— 而 score 才是每日成績單入口。
2. crosssec 表格的 t 值是 RankIC 的 t，卻印在 spread 欄位旁。spread 自身 t_adj = 2.25，
   依自訂判準會落在「調整後仍顯著」，**與原結論相反**。需正名或改算。
3. `feature_hash` 無法事後驗證：panel 在 .gitignore 內且每日重建會回溯改寫。
4. 停牌時 horizon 失真（2327 停止買賣期間，「5 日預測」用 12 個市場日結算）。
5. 1/n 分割判斷：n≥6 後帶狀重疊，−72%~−90.5% 的跌幅都會被判成分割；
   f>1 側（減資／併股）永遠判不到。
6. workflow 的 `git add` 沒有加 `config/universe/`，每日快照不會進版控。

## 待辦

1. **建 GitHub repo**（權限機制擋下，需使用者執行）：
   `gh repo create stock-lab --public --source=. --push`
2. **設 LLM API key**（secret `ANTHROPIC_API_KEY` 或 `GEMINI_API_KEY`）。
   src/models/llm.py 的 prompt 已載入與人工判斷相同的四面向框架。
3. **補財報**：FinMind `TaiwanStockFinancialStatements` 已驗證可用，
   2026-09-17 抓取時免費額度用盡。財報 point-in-time 須用法定公布期限
   （Q1→5/15、Q2→8/14、Q3→11/14、Q4→次年3/31）。
4. **等 3 個月**再看成績單。

## 踩過的坑

- TWSE rwd 端點限流：307（重導「因為安全性考量」）→ 428 → IP 冷凍。歷史走 FinMind。
- FinMind：還原股價要付費，但**除權息 TaiwanStockDividendResult 免費**可自行還原；
  每小時請求上限，抓 300 個就撞到。月營收 `create_time` 舊資料是空字串。
- Google News 只查「台積電」前三則是員工緋聞，必須加財經關鍵字。
- 融資餘額有制度性斷層（除權息停券後恢復會真實跳增數倍），守恆測試只能檢查事件日。
- 面板的 `.row` 是 `<button>`，只寫 `font-family:inherit` 會讓文字用瀏覽器預設純黑，
  深色模式下整個列表隱形。
