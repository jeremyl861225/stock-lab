# 判斷錯誤型態清冊

**每天做判斷前必讀。** 這裡累積的是三輪獨立審核實際查出的錯誤，
不是理論上的注意事項。每一條都真的發生過，而且多數不會拋任何錯誤 ——
它們只會讓判斷安靜地建立在錯的前提上。

新發現的錯誤型態請追加到對應分類，並註明日期與具體案例。

---

## A. 時間錯置（最容易犯，後果最嚴重）

**A1　用前一天的敘事解釋當天的行情**
2026-09-16：整套市場判讀寫「台股失守 46000、費半重挫 6%、防禦性撤退」，
但那是 9/15 的事。基準日當天實際是**等權 +1.21%、39/50 檔上漲、台積電完成填息**。
→ **對策**：下判斷第一件事就是用 panel 算當日等權報酬與上漲檔數，不要從新聞標題起手。

**A2　把久遠的公司行動當成「剛發生」**
2026-09-16：國巨寫「剛完成 1:4 分割、技術指標失真，不做方向判斷」。
實際分割在 **2025-08-25**（一年多前），近 60 日 `adj_factor` 恆為 1.0。
真相是 7 月連續 13 次跌停級摜壓、-53% 的真實崩跌，被這個錯誤從判斷中整個移除。
→ **對策**：宣稱任何公司行動前，用 `panel` 查 `adj_factor` 的實際變化日期。

**A3　引用盤後或次日的資訊當成當日成因**
2026-09-16：台股判讀引用「費半衝高 1.7%、台指期夜盤站穩 46K」解釋當天上漲，
但那是台股收盤**之後**的美股盤中。
→ **對策**：新聞的 `pub` 時間要對照台股 13:30 的界線。

**A4　視窗標籤錯置**
AAPL 寫「5 日再漲 7.4%」，7.4% 其實是 `ret_20`；
金像電寫「融資 20 日 +16%」，實際 `margin_chg_20` 是 +19.5%。
→ **對策**：寫「N 日」時，逐一確認引用的是 `ret_N` / `margin_chg_N` 那一欄。

---

## B. 極值宣稱（第二高頻）

**B1　「最高／最低／唯一／全場」沒有排序驗證**
三輪共查出 16 個錯誤的極值宣稱。典型案例：
- 玉山金「投信買超最強」→ 實際第 **7** 名
- 第一金「RSI 全場最高」→ 第 **2**（台灣大更高），且同一份文件把兩檔名次對調
- META「20 日全場最強」→ 第 **2**，而 **同一份 MACRO 自己就寫了 CRM +27.8% 更高**
- BRK-B「日波動最小」→ 第 **2**，而 **同一份文件的 VOO 那條寫著「波動 0.5% 最低」**
- UNH 與 PG 都宣稱「營收全場最弱」→ 互相矛盾，且兩者都不是最弱
→ **對策**：任何極值詞一律用 `sort_values` 驗，並檢查同一份文件內有無自相矛盾。

---

## C. 歸因錯誤（最難自己發現）

**C1　用落後指標否定前瞻催化劑**
- 欣興：寫「ABF 需求未見轉機」，實際是**被搜索／洗產地的司法事件**，營收其實 +56%
- QCOM：寫「營收年減 4% 卻漲 17.8%，價格與基本面背離」，
  實際有 **Amazon AI 晶片合作案**（6 則新聞有 4 則指向它）
→ **對策**：跌幅或漲幅前十檔、以及所有 high/medium 判斷，**逐檔讀新聞檔**再下筆。

**C2　用錯的變數當基本面**
金控的「月營收」受保費與會計認列擺佈，與獲利脫鉤。
2026-09-16 用它斷定「金融股漲幅缺乏基本面支撐」，但同資料夾的新聞是
**13 家金控前 8 月狂賺 5,789 億創新高**（台新新光金獲利年增 222%、富邦金為獲利王）。
整套金融股論述因此錯誤，包含當時唯一的 high conviction。
→ **對策**：不同產業的基本面指標不同。金控看獲利／EPS／CSM，不看月營收。

**C3　把有基本面的買盤說成純資金行為**
中華電寫「外資買超為資金停泊」，實際 8 月營收年增 25.6%、EPS 創 15 年新高
（而且 `rev_yoy` 就在 briefing 裡，判斷卻沒引用）。
→ **對策**：斷言「無基本面」之前，先看 briefing 的 `rev_yoy` 那一欄。

---

## D. 單邊採證

**D1　與判斷方向相反的極端值被略過，且集中在信心最高的檔位**
2026-09-16：第一金 p=0.35（全書最空、唯一 high），卻未提
外資買超 pct90、投信買超 **pct96（全場第 2 高）**；
華南金未提融資 20 日 **-70.5%（全場最乾淨的去槓桿）**。
→ **對策**：寫完每一條看空（或看多），反問「有沒有前 15% 的極端值是反方向的」。

---

## E. 數字錯誤

**E1　數字串到別檔**
緯穎寫「外資賣超 0.28」，那是**台塑化的值**；同一檔的「融資 20 日 +160%」實際是 **-12.83%**（方向相反）。
→ **對策**：每個數字寫完當場對 briefing 那一列，不要憑印象。

**E2　立論的統計被誇大**
美股 MACRO 宣稱「53 檔中 15 檔以上 RSI 落在 30–41」，實際 **12 檔**；
RSI<30 的是 **0 檔**、中位數 49.5、QQQ 47.8、VOO 46.9 都在中性區。
「廣泛超賣」這個撐起 40/53 檔看多傾向的前提，本身被高估。
→ **對策**：MACRO 裡的每個數字都要能重算。

---

## F. 部位沒有實作論述

**F1　嘴上說的維度，機率沒有反映**
v1 宣稱「價格與基本面背離」，但金融股的 p 與 RSI 相關 **-0.75**、
與營收相關 **-0.23 且方向相反** —— 部位純粹在賭超買回歸，與論述無關。
→ **對策**：寫完判斷跑一次相關性檢查：p 與你宣稱的主導維度，方向對不對？

---

## G. 方法論

**G1　不要用有偏誤的樣本校準參數**
實測顯示上漲時平均幅度 1.075σ 高於理論 0.798σ，看似該調高係數。
但那個右偏來自**生存者偏差**（universe 是今天的市值前 50，兩年等權 +272%）。
照實測校準等於把選股偏差固化進模型。
→ **對策**：在生存者偏差修好之前，維持理論值。

**G2　肥尾是真的**
48,898 筆實測超額峰度 **+4.86**，跌破 -2.33σ 的實際比例是理論的 1.5 倍。
→ 極端事件比常態假設預測的多，停損設在 -1.28σ 是偏保守的，這個方向沒錯。

## 2026-09-17 ｜「95% 信賴區間全看漲」是算術上不可能的

**問題**：能不能找出漲跌幅 95% 信賴區間都在零以上的標的（只有 5% 機率會跌）？

**答案：不能，而且不是資料不夠。**

「第 5 百分位 > 0」等價於 `μ > 1.645σ`，也就是**該期間的夏普比率 > 1.645**。
換算年化：一年期要 1.65、20 日要 5.84、5 日要 11.68。
頂尖避險基金的「整個投資組合」長期年化夏普約 1–2，那是幾百檔分散後的結果。

在本系統的模型結構下更是封死的：
```
exp_ret = 0.85σ(3.2p − 1.66)，要 > 1.645σ 需要 p > 1.124
```
機率不可能大於 1，所以**不存在任何參數組合**能產生這種區間。

**最重要的一課在實證這一段。**

| 樣本 | 獨立樣本數 | 第 5 百分位 > 0 |
|---|---|---|
| 2024-07～2026-09（兩年，全程多頭） | 約 1 | **33/50 檔** |
| 2013～2026（13 年，含 2018 貿易戰／2020 疫情／2022 空頭） | 12.4 | **0/12 檔** |

兩年樣本的 256 個重疊視窗不是分佈，是同一條上漲路徑被量了 256 次。
如果只跑兩年資料就回答這個問題，會得到「有 33 檔標的幾乎穩賺」的結論 ——
這是本專案至今最漂亮的一個假訊號。

**13 年實測（250 日報酬）**：勝率中位 66%，最高是台積電 85% 且 q05 仍為 −14.2%；
中鋼 34%、台塑 50%。一年下跌的機率約三分之一，不是 5%。

**要接近 95% 需要換維度，不是換標的**（等權 11 檔組合）：

| 持有 | 勝率 | q05 | 獨立樣本 |
|---|---|---|---|
| 1 年 | 79% | −14.9% | 12.3 |
| 2 年 | 92% | −2.3% | 5.7 |
| 3 年 | 100% | +19.3% | 3.4 |

3 年以上的數字只有 3.4 個獨立樣本，同樣不可盡信 —— 但方向明確：
**是時間在降低虧損機率，不是選股。**

分散的效果是降波動不是消滅下檔：年化波動 28% → 18%，但 q05 仍是 −14.9%。

**待查的校準問題**：本系統一年期的中性錨點設 0.55、上限 0.64，
理由寫的是「大盤 12 個月為正約 65–70%，個股低於大盤」。
但 13 年實測顯示這批大型股的勝率中位是 66%，並未低於大盤。
錨點可能偏低。（注意：這 12 檔是今日的 0050 成分股，仍有生存者偏差，
要用 PIT 宇宙重驗才能下結論。）

---

## 三個讓資料靜默失真的管線缺陷（2026-09-17 修）

三個都不會報錯、不會讓測試變紅，只會讓資料悄悄少一塊或成績單偏樂觀。

**1. 盤前抓到的空結果被當成「非交易日」永久釘死**

`collect/twse.py` 的快取判準只看檔案存不存在。排程在台北時間 01:25 跑過一次，
那時當日資料還沒上架，TWSE 回空陣列 → 空 payload 落盤 → 當天稍晚重跑時
快取命中，直接回傳空。2026-09-17 就是這樣整天被當成非交易日
（TWSE 實際有資料：加權指數 46,288.00，+0.96%）。

修法：空 payload 只有在「該交易日台北 15:00 之後才抓到」時才算數，
否則視為未抓。真正的非交易日在收盤後重抓仍是空，那次的快取才會被保留，
不會每天重打官方站台。

**2. `predict.py` 把 panel 縮回今日成分股，抹掉生存者偏差的防線**

`features/panel.py` 的 `__main__` 用 `universe.codes_ever()` 建 panel，
註解寫得很清楚：不這樣做就會有生存者偏差。但 `predict.py` 在下游又呼叫
`panel_mod.build(codes)`，`codes` 只有今日 50 檔成分股，而 `build()` 會**寫回**
`panel.parquet` —— 於是 17 檔「曾入選、後來掉出去」的股票被整批抹掉。

接著 `settle.py` 讀這份縮水的 panel，那些股票尚未到期的預測全部變成 `missing`，
再也不會被結算。本次實際發生在 6669（掉出前 50），13 筆預測失聯。

**掉出成分股的多半是跌下去的那些，正是模型看錯的樣本。**
讓它們靜默消失，成績單只會愈看愈準。

修法：`predict.py` 建 panel 時用 `codes_ever() | codes`；
預測範圍本來就另有 `feats[feats["code"].isin(codes)]` 限定，不受影響。

**3. `rev_yoy` 欄位撞名，讓 `ranking.py` 壞了好幾天沒人發現**

`features.build()` 已輸出 `rev_yoy`（台股月營收 YoY，以法定公告期限為可用日），
`fundamentals()` 也有同名欄。merge 用預設後綴 → 變成 `rev_yoy_x` / `rev_yoy_y`，
briefing 裡**沒有** `rev_yoy` 這一欄，`ranking.py` 讀它就 KeyError。

之所以拖了幾天：連續幾次排程都走「當日判斷已完成」的冪等路徑，只跑 `panel.py`
沒跑 `ranking.py`。而 `finalize` 不允許 ranking 失敗，下一次真要做判斷時
會直接中止整條流程。

修法：merge 改 `suffixes=("", "_fund")`，台股保留 PIT 明確的 `build()` 版本，
美股（`build()` 無月營收）才落到 fundamentals。

**共同教訓**：冪等路徑跳過的步驟，也跳過了它們的失敗。
偶爾要把完整流程跑一遍，否則壞掉的那一段會一直安靜下去。

---

## H. 資料欄位本身失真（2026-09-17 新增分類）

**H1　排序的兩端都不是真實極端時，該欄位就是壞的**
2026-09-17 掃描美股極端值時發現 `PBR` 欄位不可用：
TSM 顯示 **88.05**，而同一家公司的台股 2330 同日是 **9.78**（差約 9 倍）；
同時兩邊的 PER（32.0 vs 28.1）只差一成四、與 ADR 溢價相符 —— 故 PER 正常、**PBR 失真**。
另有 BRK-B 顯示 **0.000975**（實際約 1.6，小三個數量級）；
ABBV **-78.60**、PM **-34.59** 則是股東權益為負所致，數值不算錯，
但負值在排序時會沉到最底（ABBV 被排成 52/52），**極易被誤讀成「全場最便宜」**。
→ **對策**：引用任何估值欄位之前，先看它排序的**兩端**是否為可信的公司。
兩端都不合理就是欄位壞了，不是市場給了機會。
跨市場同一家公司（TSM/2330）是現成的對照組，可直接交叉驗。
→ 本日判斷已全面迴避 PBR 並在 MACRO 註記；管線未修（見 `data/revisions.jsonl`）。

**H2　沒被引用過的壞欄位不會拋錯，也不會被發現**
PBR 失真至少已存在數日，但因為前幾版判斷剛好都沒引用它，
既沒有測試變紅，也沒有任何症狀。它只會在某天有人寫下
「PBR 全場最低、估值有支撐」的時候，安靜地把一個錯的前提送進判斷。
→ **對策**：每日的極端值掃描要**看完所有欄位的兩端**，
而不是只看這次打算引用的那幾欄。

---

## 2026-09-18 ｜升息循環重啟：過去兩年的基本率不再適用

2026-09-16 Fed 升息一碼至 3.75–4%，為三年來首次升息，點陣圖指向今年再一碼，
10 年期公債殖利率升抵 5.01%。這是本專案開始以來第一次遇到**制度轉折**。

**為什麼這件事對判斷的影響大於任何個股消息**：
本系統的機率錨點（台股 20 日 0.58–0.68）是從一段**全程多頭**的樣本得來的，
而 `LESSONS G1`／`2026-09-17` 那節已經證明那段樣本的獨立樣本數約等於 1。
升息循環重啟之後，那個錨點失去了它原本就很薄弱的依據。
→ **對策**：本日美股的中性錨已下修至 0.52（v3 為 0.50–0.55 但偏多檔數較多），
p 分佈中位 0.510、範圍 0.47–0.56。台股的錨點尚未依此檢討，**這是待辦**。

**一個具體的判讀陷阱（本日差點犯）**：
升息日的隔天（9/17）是**反彈日** —— 等權 +0.76%、36/53 收紅、QQQ +1.73%。
若照 `A1` 的老錯誤用 9/16 的下跌敘事去寫，整份判斷的前提就反了。
而反過來，若把 9/17 的反彈當成「市場已消化升息」也一樣危險：
當日漲幅前五名（AMD／MU／ORCL／TSM／NVDA）**全部是 AI 硬體**，
這是單一主題的資金輪動，不是廣泛的風險偏好回升。
→ **對策**：反彈日要看**上漲的組成**，不是只看上漲的檔數。

**超賣不等於錯殺，這次有具體反例**：
BAC RSI 31.3、5 日 -7.0% 為全場最弱，表面上是最極端的超賣。
但成因具體：執行長於 9/14 預告第三季投銀費用年減至少一成，屬**真實獲利下修**。
前一版給 BAC 0.55／medium（全書最樂觀的金融股），本版收斂至 0.52／low。
→ **對策**：對每一檔 RSI < 35 的標的，先查「這波跌勢有沒有具體事件」，
再決定它是均值回歸的候選，還是重評價的起點。這兩者的處置完全相反。

## 2026-09-18 ｜`git add` 遇到不存在的路徑會整條失敗，而且我把它吞掉了

我在收尾時跑：
```
git add -A src judgments tests docs data/predictions.jsonl data/settlements.jsonl 2>/dev/null
git commit -q -m "9/17 預測補上；修…" && git push -q origin main && echo "✓ pushed"
```
`data/settlements.jsonl` 當時**不存在**。`git add` 遇到不存在的 pathspec 會
**整條失敗、一個檔都不暫存**，而 `2>/dev/null` 把錯誤訊息吞了。
下一行是獨立的一行（不是 `&&` 串接），所以 `git commit` 照樣執行，
提交了先前殘留在暫存區的內容（兩個 `git rm --cached` 的刪除），
然後印出「✓ pushed」。

結果：commit 訊息宣稱修了快取落後與美股歸零兩個 bug，**實際上一行程式碼都沒進版**。
我據此向使用者回報「已 push」，那是假的。

**教訓不是「別打錯路徑」，是「宣稱成功之前要驗證」。**
以後收尾一律：
1. `git add <明確列出的檔案>`（不要 `-A <多個路徑>` 混用）
2. `git diff --cached --stat` 先看暫存區真正有什麼
3. commit 後 `git show --stat HEAD` 加上關鍵字比對，確認核心修改真的在裡面
4. 不要用 `2>/dev/null` 吞掉 git 的錯誤

同一天另一個 session 也提交了 `修 finalize 的自動提交整條鏈靜默失敗`，
顯示這個模式在自動化管線裡也會發生。

## 2026-09-18 ｜CSS 變數打錯不會報錯，只會安靜地變透明

K 線的圖表框與尺度按鈕用了 `var(--card2)` —— 那個變數**根本不存在**，
是我憑空發明的。瀏覽器不報錯、不警告，只是把它當成 initial 值，
於是兩個主題下框線都是透明的，而我在深色模式下沒看出來。

正確的值是 `--bg`：它在兩個主題下都比卡片更「凹」一層
（淺色 bg #F8F7F2 vs card #FFFEFC；深色 bg #111214 vs card #181A1D）。

已加測試 `test_no_undefined_css_variables`：掃 `var(--x)` 與宣告處比對，
inline style（如橫桿的 `style="--o:left"`）也算宣告。

---

## 2026-09-19 ｜同一天裡兩個融資極值，一個是訊號一個是假象

`margin_chg_20` 排序的前兩名，性質完全相反：

| | margin_chg_20 | 融資餘額變化 | 自身 60 日中位 | 判定 |
|---|---|---|---|---|
| 第一金 2892 | **+599.09%**（全場最高） | 2,318 → 16,205 張 | 2,410 張 | **真實暴增** |
| 台灣大 3045 | +132.76%（全場第 2） | 635 → 1,478 張 | 592 張 | **基數假象** |

全庫當日融資餘額中位是 8,417 張。台灣大的餘額一天之內可以從 67 張跳到 766 張
再掉回去 —— 存量型欄位不該有這種行為，它只是基數太小，百分比自然劇烈。
第一金則是實打實的槓桿追價：餘額衝到自身中位的 6.7 倍。

2026-09-16 與 09-17 兩版都各自標記過台灣大一次，但都寫成個案備註，
沒有形成可套用的判準，於是每天都要重查一遍。

→ **判準（引用任何 `margin_chg_*` 之前必跑）**：
  1. 回 `panel` 取該檔的 `margin_bal` 絕對量（今日、N 日前、自身 60 日中位）
  2. 絕對量遠低於全庫中位（本日為 8,417 張）且自身中位也低 → 基數假象，不得引用
  3. 餘額衝上自身 60 日中位數倍 → 真實訊號
  電信股（2412／3045）融資餘額本來就只有幾百到兩千張，是假象的高風險區。

## 2026-09-19 ｜「同處最低區」這種措辭會被當成極值宣稱

美股 HD 的 RSI 是 29.70、BAC 是 29.66，差 0.04。我寫成
「RSI 29.70 與 BAC 同處全場最低區」—— 語意上沒說錯，但
`verify_judgment.py` 把它判為「宣稱 rsi_14 最低」，並同時觸發
「BAC 與 HD 同時宣稱最低」的自我矛盾。

查核器會誤讀，讀者也會。→ **一律寫名次**：
「RSI 29.70 為全場第 2 低（最低為 BAC 的 29.66，兩者僅差 0.04）」。
兩檔幾乎相同時，把差距一起寫出來，比含糊帶過更誠實。

## 2026-09-19 ｜盤前跑 prepare，`collect/universe.py` 必然失敗，那是防線不是故障

排程在台北時間早上跑時，當日台股尚未開盤，`universe.build()` 取到 0/50 檔，
於是丟 `RuntimeError: 只取得 0/50 檔，拒絕覆寫既有 universe`，
`prepare` 的摘要會印出「collect/universe.py ✗ 失敗（returncode 1）」。

**這是刻意的**：它擋的是「用空資料覆寫掉既有 universe」。
其餘九個步驟照常完成，44 項測試全過，`as_of` 仍正確指向前一交易日。

→ 看到這一行不要去修，也不要因此中止流程。真正該停下來的是**測試失敗**。
判斷方式：摘要中除了 universe 以外還有別的 `✗`，或末段的 pytest 不是全綠。

## 2026-09-19 ｜重算了衍生欄位，卻沒重算從它導出的標籤

`roll_1y.roll()` 每天把一年期論點重新定價：`prob_up`、`exp_ret`、
`up_magnitude`、`dn_magnitude` 全部重算。但輸出那一段是

```python
**{k: j[k] for k in ("code", "stance", "thesis", ...)},
```

`stance` 被原樣沿用。它是從 `exp_ret` 的正負導出的標籤，
於是只要重新定價讓 `exp_ret` 跨過 0，標籤就和數字對不上。

本日實際發生於 2395／4958／3443：`p=0.527`、`exp_ret=+0.0115`，
`stance` 仍寫 `bearish`。`claude_judgment.load()` 的方向一致性檢查擋下整份檔案，
`daily.py finalize` 在 ingest 階段中止 —— **這次是好的失敗**：
它吵得夠大聲，所以被發現了。若沒有那道檢查，面板上會出現一批
「看空但期望值為正」的紀錄，而且不會有任何症狀。

→ **通則**：重算一組欄位時，把所有由這組欄位導出的欄位一起重算，
不要用 `**{k: j[k] for k in (...)}` 這種「其餘照抄」的寫法夾帶衍生欄位。
照抄的欄位只能是真正的輸入（論點、事實、否證條件），不能是結論。

## 2026-09-19 ｜`finalize` 的自動提交白名單不含 `src/`

`daily.py finalize` 的提交白名單是 `docs/`、`judgments/`、`config/`、
`data/predictions.jsonl` 等產物路徑，**沒有 `src/`**。

本日修好 `src/roll_1y.py` 之後跑 finalize，它照常 commit 並 push，
訊息是「每日預測 2026-09-18」，看起來一切正常 —— 但那個修正一行都沒進版。
若當時就回報「已修好並推上去」，那句話是假的（與 2026-09-18 那次同型）。

當天的輸出裡還有一行 `error: cannot rebase: You have unstaged changes.`，
那就是它在講這件事，只是不顯眼。

→ 改到 `src/` 底下的任何東西，finalize 之後要自己補一次：
`git add <明確檔名>` → `git diff --cached --stat` → commit →
`git show --stat HEAD` 確認檔案真的在裡面 → push → `git status -sb` 確認已同步。

## 2026-09-19 ｜檢查點在寫下當天就已經是假的

美股一年期第一版一產出，就有三檔顯示「論點動搖」：VZ、QCOM、META。
去看被推翻的那一條，全都是我自己寫壞的：

- 規則產生器對每一檔都追加「單季營收年增維持正值」，門檻一律寫 0。
  VZ 當時是 −0.7%、QCOM 是 −4.0% —— 那條前提在下注當天就是假的。
- META 我手寫「EPS 年增 ≥ −10%」，而它當時已經是 −13.4%。門檻寫在現值之上。

前提的意義是「**接下來**若這件事翻掉，代表我錯了」。若它在下注當天就是假的，
它不是前提，是我把一個現成的事實寫成了預測。更糟的是它會讓面板一開張就顯示
三檔「動搖」，把真正的推翻訊號淹在噪音裡 —— 這個機制的全部價值就在於
「亮起來的時候一定有事」，摻了假警報就等於關掉它。

→ 兩件事同時做：
1. 門檻一律相對現值。成長率沒有上下界，統一寫 0 會同時犯兩種錯：對年增 +85%
   的公司太鬆、對年增 −4% 的公司則是出生即死。現在分三段：
   >20% 要成長腰斬、0–20% 要轉負、已衰退則要再惡化 5pp。
2. 出貨前擋下來。各 build 腳本加 `_audit_day_one()`，用基準日重評一次所有
   檢查點，有 broken 就 `sys.exit(1)` 不產檔。

**但「出生即死」不全是錯的**：轉機型論點（「EPS 年增轉正」）的前提本來就
從假開始，那正是論點的內容。台股 2454 就是這一型，我之前把它讀成
「機制當場抓到我的判斷」，其實只對了一半。所以 checkpoints 加了
`expects="turn"`：標記過的算「未達成」而非「已推翻」，沒標記的才進 day-one 關卡。
寫判斷時被迫講清楚「我賭它維持」還是「我賭它翻過來」，是這條的附帶收穫。

## 2026-09-19 ｜負的股東權益會把 ROE 的正負號整個翻過來

ABBV 的股東權益是 −59 億美元（長年大額買回加分拆的結果），
ROE 照算出來是 −206%。它其實是穩定獲利的公司。

危險在於這個數字進了橫斷面百分位之後：負分母讓「最賺錢」變成「排最後」，
而排序本身看起來完全正常，不會有任何一欄是 NaN 讓人起疑。
本清單裡權益為負的還有 PM，在標普 500 裡這類公司不少（HD、MCD、SBUX…）。

→ 平均股東權益 ≤ 0 時 ROE 一律作廢，缺值好過錯號。同一條也適用於
任何「分母可能為負」的比率 —— 台股那邊已經處理過分母趨近零，
但「趨近零」與「小於零」是兩種不同的壞法，前者放大、後者翻號。

## 2026-09-19 ｜一次性利益會讓本益比看起來只有一半

Alphabet 2026Q2 帳面淨利 1,121 億美元、年增 298%，其中約 **990 億是權益證券
的未實現評價利益**。同期營業利益是 407.7 億、年增 30%。

於是帳面本益比 17.4 —— 在這份 50 檔的清單裡屬於最便宜的一端，
而用稅後營業利益重算是 36.9，落在偏貴的一端。差了一倍以上，
而 GOOGL 佔本清單權重 8.8%，是第三大。

→ `us_fundamentals` 加 `nonop_heavy` 旗標（|淨利| > 2×|營業利益|）與
`eps_op_ttm`（稅後營業利益每股）。命中的公司在規則模型裡改用營業利益本益比，
並在論點文字裡寫明換過。本批命中三檔：GOOGL、MRK、TSLA。

台股那邊還沒做同樣的檢查，FinMind 的 EPS 一樣含業外 —— 待辦。

## 2026-09-19 ｜排程工作流複製了一次管線，然後安靜地落後

`.github/workflows/daily.yml` 原本把 collect → panel → briefing → predict →
settle 一步步展開寫死。而 `src/daily.py` 後來陸續加了新聞收集、K 線、
美股季報、一年期滾動。兩邊於是分岔：本機跑出來的面板有一年期論點與 K 線，
排程跑出來的沒有。**沒有任何一個環節會報錯**，兩邊都「成功」。

→ workflow 改成直接呼叫 `python src/daily.py prepare`。流程只能有一個定義。

同一天發現的同型問題：`docs/charts.json` 的美股部分讀的是一次性回補的
`prices_5y.parquet`，而每日流程更新的是 `prices.parquet` —— 美股 K 線的
最後一根停在回補當天（09-16），卡片卻已經是 09-18 的收盤。
兩份資料、一個顯示、沒有對帳。現在合併兩份並加測試比對面板的 as_of。

## 2026-09-19 ｜測試把正式的狀態檔寫壞了，而且是靜默的

`tests/test_roll_1y.py` 的 fixture 只把 `roll_1y.STATE` 導到 tmp_path。
但 `roll(commit=True)` 還會呼叫 `news_gate.scan(commit=True)`，
那寫的是正式的 `data/news_seen.json`。跑一次測試就把 228 則真新聞
標記成「已看過」—— 之後真的出現併購也不會再觸發。

測試汙染正式狀態，測試本身照樣全綠。

→ fixture 要導**所有**會被該函式推進的持久化狀態，不是只導最明顯的那一個。
寫 fixture 時的問法應該是「這個函式會寫哪些檔」，而不是「這個模組有哪些常數」。

## 2026-09-19 ｜改了雜湊的輸入，等於把所有既有基準線作廢

把 `roll_1y._basis()` 從 `parts = [code]` 改成 `parts = [market, code]`，
只是為了「看起來完整」。結果是所有既有雜湊在改版當天全部失配 ——
下一次滾動會把 38 檔台股全部判成「財報已更新」而重算 P漲，
實際上什麼都沒更新，而帳本會把那 38 筆記成新判斷。

狀態檔本來就是分市場存的，market 那一格是多餘的。

→ 雜湊的輸入只放真正會改變判斷的東西。任何一次改動都要先問：
既有的基準線會不會全部失效？若會，要嘛不改，要嘛連帶寫一次遷移。

## 2026-09-19 ｜同一個循環的兩檔股票，被判到相反的兩端，差別只在覆蓋率

新聞閘標記 2344 華邦電（併購英飛凌記憶體事業），我去讀那幾則新聞，
結論是併購本身落在窗口外（2027 下半年完成）、窗口內幾乎不影響財報。
但讀的過程裡發現一件更嚴重的事：

- **華邦電**（規則推導）：毛利率較四季前 +43.6pp（第 98 百分位）、
  EPS TTM 年增 +1232%（第 100 百分位）、本益比 19.8（便宜度第 78 百分位）
  → P漲 0.621，台股一年期期望值 +23.4%，排最前段。
- **美光**（同一天我手寫）：毛利率 84.6%、EPS 暴增、本益比 22.9
  → P漲 0.48，美股一年期期望值 −5.9%，排最後。
  理由寫得很清楚：「記憶體的歷史教訓是本益比最低的時候最貴 —— 分母是週期高點的獲利」。

同一個記憶體超級循環，同一組財報特徵，我把一檔判到最前段、一檔判到最後段。
差別不在判斷，在**有沒有人讀過它**。

根因：橫斷面百分位對「這是循環的哪個位置」沒有辨識力。它看到第 98、第 100
百分位，看不到那是均值回歸前的最後一哩。而規則模型覆蓋的是**大多數**標的，
手寫的只有少數幾檔 —— 所以這種不一致是系統性的，不是偶發。

→ 短期：2344 已修訂（P漲 0.621 → 0.50，並補一條負債比檢查點，
因為全現金 11.2 億美元的支付承諾會在窗口內先產生融資需求）。
→ 待辦（比這次修訂重要）：規則模型需要一個「週期位置」的修正項。
可行的判準是「當期毛利率相對該公司自身歷史分布的百分位」——
落在自身歷史第 95 百分位以上時，把成長類訊號的權重調降甚至反號。
目前 `models/rule_1y.py` 全部是**橫斷面**百分位，沒有任何**時間序列**上的參照，
這是它最大的結構性缺口。

→ 通則：手寫論點與規則論點在同一張排序表上並列時，要定期抽查
「同產業裡，被規則排到最前段的那幾檔，是否違反我自己手寫過的論點」。
不一致本身就是訊號 —— 它指出規則少了哪一個維度。

## 2026-09-19 ｜我寫的測試只在本機跑得動，CI 連紅三次；改完又寫成空轉

`tests/test_panel_render.py` 新增的 K 線對帳測試直接讀
`data/features/panel.parquet` —— 那個檔在 `.gitignore` 內。
本機 85 條全過，CI 連三個 commit 都是 `1 failed, 76 passed, 8 skipped`，
使用者的手機因此收到三次失敗通知才發現。

**第一個錯**：一個只在本機跑得動的測試，等於在唯一會擋住錯誤的地方缺席。
寫測試時要先問「CI 的 checkout 裡有這個檔嗎」。
判準很簡單：`git ls-files --error-unmatch <路徑>`。

**第二個錯（比第一個嚴重）**：改成讀版控裡的 `data/briefing.parquet` 之後，
我寫了 `if not isinstance(want, str): continue` —— 而 briefing 的 `as_of`
是 `datetime64`，不是字串。迴圈於是跳過**每一個**市場，一次比對都沒做就回報通過。
故障注入（把美股 as_of 改回 09-16）時它照樣綠燈，才發現。

空轉的測試比沒有測試更糟：沒有測試至少不會讓人以為有防線。
這與 README 記載的 `test_truncation_invariance` 是同一型 ——
那支比較 `f(trunc(X))` 與 `f(trunc(trunc(X)))`，數學上不可能失敗，
卻被寫進 README 當成第一道防線。**這個系統已經犯過兩次同型錯誤。**

→ 兩條規則，從現在起套用到每一支新測試：
1. **迴圈裡有 `continue` 的測試，必須數它實際比對了幾次並斷言下限。**
   本例加了 `assert compared >= 2, "這個測試在空轉"`。
2. **新測試一律做一次故障注入**：故意把它該抓的東西弄壞，確認它真的會紅，
   再把東西還原。沒紅過的測試不算寫完。

## 2026-09-19 ｜判斷賭的是反轉，而這段樣本裡反轉的 IC 是負的

把 2026-09-18 台股 50 檔的手寫 `p20` 對簡報欄位做 Spearman：

| 欄位 | ret_20 | rsi_14 | dist_high_60 | rev_yoy | short_ratio |
|---|---|---|---|---|---|
| 與 p20 的相關 | **−0.62** | −0.59 | −0.52 | **+0.62** | −0.44 |

六個欄位的 rank-R² 是 0.66，而新加的 `mech_core` 基準線
（0.5×20 日反轉 ＋ 0.5×營收年增）與手寫 p20 的 Spearman 高達 **+0.736**。
也就是說**手寫判斷有三分之二可以用兩句話複製**：跌深、營收強。
（美股只有 −0.03 —— 美股沒有月營收，那一半的資訊源根本不存在。）

接著用 PIT 宇宙（`config/universe/` 的 26 份快照，511 個交易日，
避開「今天的前 50」那個生存者偏差）量這兩半各自的歷史 IC，重疊調整後：

| 訊號 | h=5 | h=20 | h=60 |
|---|---|---|---|
| 營收年增 | IC +0.069（t +2.69） | +0.129（t +2.42） | +0.188（t +2.32） |
| 反轉（−ret_20） | IC −0.045（t −1.65） | −0.074（t −1.54） | −0.042（t −0.46） |

**營收年增是唯一 |t| > 2 的訊號；反轉在三個期間的 IC 全部為負。**
翻過來說，這段樣本裡有效的是動能不是反轉 —— 而判斷正在賭反轉，
與 `momentum` 基準線的相關是 −0.62（鏡像）。

→ **對策**：
1. 寫「跌深＝機會」之前，先問這一檔的跌是不是有具體事件
   （已有 `2026-09-18 BAC` 那條：超賣不等於錯殺）。跌深本身沒有統計支撐。
2. 營收年增是目前唯一有證據的橫斷面訊號，引用它時要確認是 `rev_yoy` 那一欄本身，
   而不是從價格反推的印象。
3. 每天的判斷寫完，跑一次 `p` 與 `ret_20`／`rev_yoy` 的相關，
   確認自己這批的反轉權重是不是又悄悄變大了。

**但這段證據本身也很薄，不要當成鐵律**：樣本只有兩年、全程是 AI 硬體循環的多頭，
動能在這種期間天然佔優；反轉的 |t| 約 1.5，只能說「沒有被證實」，
不足以說「反轉是反指標」。真正的判準是上線後 `src/attribution.py` 的選股分項。

**順帶驗掉一個猜測**：原本懷疑「反轉在一週最強，所以 5 日的 p 不該由 20 日
除以 2 推導」（METHOD §4.2）。實測不支持 —— 營收年增的 IC 由 h=5 的 0.069
到 h=20 的 0.129，接近 √(20/5)=2 倍的縮放，正是現行公式假設的形狀；
反轉在 h=5 也沒有比 h=20 強。**§4.2 維持不變。**

## 2026-09-19 ｜六家銀行同一天財報，而判斷書沒有一檔提到

事件日曆（`src/events.py`）一接上就抓到：2026-09-18 的美股判斷裡，
**JPM／BAC／MS／GS／WFC／C 六家全部在 10/13–14 公布財報**，落在 20 日視窗內，
而六份 `why` 沒有一份提到財報；COST 更只剩 **4 個營業日**就要公布。

這不是粗心，是結構問題：以往只有台積電那種「人記得住」的大事會被寫進 `why`
（2026-09-18 的 build 腳本甚至用字串替換，把 5 日版那句「10 月中法說會在視窗內」改掉）。
103 檔沒有人記得住，而系統從來沒有事件日曆可查。

**為什麼這件事重要**：視窗內有沒有財報，是兩個不同的賭局。
它同時改變實現波動（財報日的跳空）與漂移（意外的方向），
而系統對兩者給出**一樣的幅度**（`0.85 × vol_20 × √h`）。

→ **對策**：
1. `briefing.parquet` 已有 `evt_type`／`evt_days`／`evt_in_5`／`evt_in_20`／`evt_types_20`，
   判斷時要真的看。
2. `verify_judgment.py` 新增「事件視窗」查核：20 日視窗內有財報或除權息、
   而 `why` 沒提到的，逐檔列出。**月營收刻意不列** —— 台股每檔每月都有，
   逐檔警告會變成 50 項噪音，而噪音太多的警報等於沒有警報。
3. 同一天同一個產業一起公布財報（金融股尤其明顯）時，那不是六個獨立賭注，
   是一個賭注押六次。排序時要記得。

## 2026-09-19 ｜生存者偏差值多少：20 日基本率 68.4% → 61.7%

回測的評分集合原本用 panel 裡的全部 67 檔台股逐日評分，包含「當時不在前 50、
後來才漲進來」的那些。改用 `config/universe/` 的 26 份每月快照逐日還原
當時的成分股（`universe.load(as_of)`）之後：

| | 舊（今天的市值前 50） | 新（PIT 成分股） |
|---|---|---|
| 台股 5 日上漲基本率 | 58.6% | **53.6%** |
| 台股 20 日上漲基本率 | 68.4% | **61.7%** |

**那 6.7 個百分點就是選股偏差本身。**

更值得記的是 `momentum` 的變化：

| | 舊 | 新 |
|---|---|---|
| 20 日準確率 | 68.4%（與 always_up 並列第一） | 61.7%（與基準線相同） |
| 5 日準確率 | 56.4% | 50.9%（僅高於 random） |
| 20 日 IC 調整 t | **2.14**（舊 README 唯一「通過」的數字） | **0.57** |

README 舊版憑推理寫的「動能策略在倖存者樣本上天然佔優」，現在有數字了。
**PIT 之後，四個市場×期間組合裡沒有任何模型的 IC 調整 t 超過 1.2。**

→ **對策**：
1. 任何「某某模型有 edge」的宣稱，先問它的評分集合是不是 PIT 的。
2. 這件事的教訓不是「動能沒用」，而是**一個看起來通過檢驗的 t 值，
   可以整個由樣本選擇造出來**。舊版那兩個 2.14 從來沒有被質疑過，
   因為它們長得就像正常的統計結果。
3. 美股仍然沒有歷史成分股快照（`config/universe_us/` 最早 2026-09-17），
   所以美股的回測列一律標 `pit=False`。造一份假的歷史快照比沒有快照更糟。

## 2026-09-21 ｜同一件事有兩套實作，錯的那套沒人看：檢查點晚了整整一個月

`checkpoints._monthly_rev()` 的可用日算成「FinMind 的 `date` 欄 + 一個月 + 9 天」。
但 FinMind 這張表的 `date` **已經是營收月的次月**（8 月營收那一列
`date=2026-09-01`、`revenue_month=8`），8,419 列無一例外。於是可用日比法定公告日
晚了整整一個月，**每一條 `rev_yoy`／`rev_yoy_ttm` 檢查點都在用上上個月的營收對帳**。

`features/panel.py` 用 `revenue_year`／`revenue_month` 自己重算，是對的。
兩邊對「今天看得到哪個月」各有一套定義，而錯的那套從來沒有人正面讀過它的輸出 ——
它只在別人問「這條前提倒了嗎」的時候回答，而答案一直是用過期資料算的。

代價是**假警報淹掉真警報**：修正後 38 檔台股裡 3 檔的「動搖」直接消失
（3037 欣興、3008 大立光、1326 台化 —— 8 項警報裡的 3 項），
而那三條前提根本沒倒。3037 的 TTM 年增實際是 27.83%（門檻 25%），
被誤讀成 23.26%；它同期的月營收年增是 **+56.3% 創新高、ABF 報價優於預期**，
若照舊資料寫判斷，會把一個正在加速的基本面寫成「前提被推翻」。
這個機制的全部價值在於「亮起來的時候一定有事」。

→ 已修（`df["avail_date"] = df["date"] + pd.Timedelta(days=9)`），
  並補 `tests/test_checkpoint_availability.py` 三條：釘住 FinMind 的 `date` 語意、
  釘住可用日、**釘住 checkpoints 與 panel 對同一個月份的一致性**。
  三條都做過故障注入（把舊算法放回去，2 條轉紅）。
→ 通則：**同一個概念有兩處實作時，測試要釘的是「兩邊一致」**，
  而不是各自對自己的實作斷言 —— 各自斷言的話，兩套都會「通過」。

## 2026-09-21 ｜「財報更新重算 36 檔」那天，財報一個字都沒變

`roll_1y._basis()` 把**本益比**放進雜湊（取到小數一位），
註解寫的理由是「避免每天因為 0.01 的變動就重算」。但本益比動輒二十到兩百，
一天 1% 的漲跌就足以推過第一位小數 —— 於是價格幾乎天天在改雜湊。

本日實測：9/18 → 9/21 之間，季報快照 **0/67 列有變動**、月營收 **0/68 列有變動**，
而滾動印出的是「僅重新定價 0 檔 · **財報更新重算 36 檔**」。
36 檔全部是本益比的小數位在動。P漲 也真的跟著改了（2408 由 0.631 → 0.557，
−0.074），因為規則模型的「便宜度」用的是橫斷面本益比百分位。

數字本身不算錯（價格漲了，便宜度確實下降），**錯的是分類與它引發的誤讀**：
DAILY.md 把三件事分得很清楚（重新定價／重算 P漲／重寫論點），
並說台股 P漲 一年只該重算約 16 天。實際上它幾乎每天都在觸發，
而回報若照著標籤寫「36 檔因財報更新重算」，讀者會以為有 36 份新財報。

→ 本日回報改寫成「36 檔因**本益比隨價格變動**而重算 P漲，財報與月營收零變動」。
→ 待辦（沒有當天改，因為改雜湊的輸入會讓所有既有基準線作廢，
  見 2026-09-19 那條）：要嘛把本益比移出雜湊、讓它歸「重新定價」管，
  要嘛把門檻改成相對變動（例如 ±3%），並連帶寫一次遷移。

## 2026-09-21 ｜機制寫好了，但一條都沒用上

2026-09-19 那條記著「checkpoints 加了 `expects="turn"`，台股 2454 就是這一型」。
今天實際去數：四份判斷檔、**111 條檢查點，標了 `expects` 的是 0 條**。
`checkpoints.py` 的實作是對的、docstring 寫得很完整，但沒有任何一份判斷用它。

於是 2454 聯發科從下注第一天起就掛著「失效　已推翻 2 條」，
而被推翻的兩條正是論點本文自己寫著「財報還沒轉」的那兩條 ——
賭它翻過來就是論點的內容。真正該擔心的（毛利率 ≥40%）一直是 holding。

→ 已修訂 2454：兩條改標 `expects="turn"`，另補一條真正的 hold 型
  （月營收年增 ≥10%，現值 +44.1%），判定由「失效」回到「成立」。P漲 維持 0.52 不動。
→ 通則：**寫完一個機制，要去數它的採用率**。一個零採用的機制在程式碼裡看起來
  和一個正常運作的機制一模一樣，而且測試會過 —— 因為測試測的是機制本身，
  不是「有沒有人用」。這與 README 那兩支空轉測試是同一型的病。

## 2026-09-21 ｜照抄舊範本，把一個沒動的判斷悄悄改掉了

修訂 2454 時照抄 `judgments/revise_1y_20260918_2344.py`，它用的是舊的兩點模型
（`base×(1±skew)`、`ev = p·up + (1−p)·dn`）。但一年期已於 2026-09-19 改由
`models/price_1y.py` 的對數常態統一導出，**沒有獨立的 skew**。

我明明寫著「P漲 維持 0.52 不變」，跑出來的期望值卻從 +3.08% 變成 +1.70% ——
因為範本把原本隱含的 +0.228 偏度歸零了。P漲 沒動、論點沒動，
只因為換了一條算式，帳本上就會多出一筆「新判斷」。

→ 已改用 `price_1y`，導出值與滾動產出的完全一致（+3.08%）。
  並在 2344 那份範本頂端加上過時警告，指向新範本。
→ 通則：**範本會過期，而過期的範本不會報錯**。照抄之前先問
  「這份是哪一天寫的？之間改過模型嗎？」；修訂後一定要比對
  「沒打算改的欄位是不是真的沒改」—— 這次是自己印出來才看到的。

## 2026-09-21 ｜盤後 18:00 跑，融資融券整欄是空的

本日 TW 的 `margin_chg_5`／`margin_chg_20`／`short_ratio` **50 檔全為 NaN**。
不是 bug：FinMind 的融資資料只到 9/18，而收資料時間是台北 18:12 ——
價格與三大法人當天下午就有，融資餘額當晚才上架。

危險在於它**不會拋錯**，而且前兩版（9/17、9/18）判斷金融股「籌碼擁擠」
的主要依據正是融資暴增（第一金 +599.09% 那條）。今天那個依據不存在，
若沒發現就沿用上一版的看空強度，等於用一個今天查不到的證據下注。

→ 本日處置：對 12 檔金控調降看空強度，並在 MACRO 與 facts 明講
  「這是證據減少，不是看法改變」，免得日後被讀成改判。
→ 通則：**每天的極端值掃描要先數每一欄的非空筆數**（H2 那條只說要看兩端，
  不夠）。整欄皆空時，排序的兩端都會是 NaN，「看兩端」看不出任何異常。

## 2026-09-21 ｜沒有新交易日的市場，不該產生新判斷

台股 as_of 是 20260921，美股是 20260918（美股當天尚未開盤）。
`prepare` 的收尾訊息一律印「判斷檔請命名為 build_20260921.py（台股）／
build_us_20260921.py（美股）」，照著做就會寫出一份 as_of=20260921 的美股判斷。

那會壞在兩個地方：
  · `settle.py` 是用 `sub["ds"] == p["as_of"]` **精確比對**進場日的。
    美股面板沒有 2026-09-21 這一列，53 檔 × 2 個期別 = 106 筆預測
    會全部變成 `missing`，永遠結不了帳（與 2026-09-17 記的 6669 同型）。
  · 資訊面上它也是同一份 9/18 的資料再下一次注，違反憲法 B 的精神。

`predict.py` 自己是對的：第 118 行 `a_of = code_as_of.get(code, as_of_str)`
逐檔用**自己市場**的交易日，所以本日只寫了 600 筆（50 檔台股 × 6 模型 × 2 期別），
美股一筆都沒補。判斷應該跟著同一條規則。

→ 本日只寫台股判斷。美股的 9/18 判斷已在帳本，不重複下注。
  一年期的滾動本來就有這道防線（「滾到跟來源同一天不是滾動，是覆寫」），
  5／20 日這一側沒有，靠人記得。
→ 待辦：`prepare` 的收尾訊息應該逐市場印各自的 as_of，
  而不是把台股的 as_of 套到兩個市場上。

## 2026-09-22 ｜自我檢查的相關係數不能估計，而我兩份判斷都估錯了方向

寫完台股與美股的判斷書之後，兩份的 MACRO 我都先填了「本批 p 與各欄的 Spearman」
的數字，然後才去算。結果兩份都錯，而且**錯的方式相反**：

| | 我填的 | 實算 |
|---|---|---|
| 台股 rev_yoy | +0.401 | **+0.294**（初稿）→ +0.316（修正後） |
| 台股 當日報酬 | −0.279 | **+0.214**（初稿，**連正負號都反**） |
| 美股 rev_yoy | +0.171 | **+0.381** |
| 美股 ret_20 | −0.481 | **−0.045** |

台股那個反號是有後果的，不只是打錯字：MACRO 寫著「本批確實在對今天漲停的那幾檔扣分」，
而實算是 **+0.214 —— 部位在獎勵今天漲停的那幾檔**。
成因是我把 ABF 三雄（當日 +9.8%～+9.9%）與日月光（+4.5%）留在錨點之上，
一邊寫著「懲罰追價」一邊給它們 0.53–0.54。這正是 `LESSONS F1`，
而且我是**自己算完才看到的**，不是被查核器抓到的（查核器不驗這一項）。

處置：把 3037／8046 收到 0.53、3189／3711 收到 0.52，當日報酬相關降到 +0.087，
然後把整段敘述改寫成實算值。

→ **通則：MACRO 裡任何一個統計數字，落筆前必須先跑出來。**
   `LESSONS E2` 記的是「立論的統計被誇大」，這一條比它更基本：
   那次是算了但誇大，這次是**沒算就先寫**。
   判準很簡單：如果一個數字是我「應該會是這樣」得來的，它就還不能寫進判斷書。

## 2026-09-22 ｜查核器用 Pearson、判斷書報 Spearman，同一件事兩個答案

`verify_judgment.check_thesis_alignment` 用的是 `.corr()`（**Pearson**），
我在判斷書裡報的是 **Spearman**。本日台股：Pearson **+0.128**、Spearman **+0.316**。
於是查核器亮起「主張看重基本面，但 p 與營收相關僅 +0.128（部位可能沒有實作論述）」，
而我自己算的數字看起來沒問題。

差距不是誤差，是有成因的：Pearson 被三個極端值支配 ——
南亞科 `rev_yoy` +561%、川湖 +344%、華邦電 +289% ——
而**那三檔正是我以「循環位置」為理由壓到 0.52–0.53 的那三檔**。
所以「名次上營收是主導維度、水準上不是」兩句話同時為真。

→ 兩個數字都要報，並說明哪一個被什麼支配。只報 Spearman 會讓一個真實的張力消失；
   只看 Pearson 則會把一個寫得出理由的取捨誤判成遺漏。
→ 待辦：查核器那一行應該同時印 Pearson 與 Spearman，否則每次都要人工解釋一遍。

## 2026-09-22 ｜「查不到」不等於「查無」

台光電 2383 本日的 `ret_5` −4.99% 與 `trust_5` −0.278 **都是全場最低**，
而 `rev_yoy` +129.8% 為全場第 7 高 —— 所有數字都指向「背離」。
9/21 版據此給了 0.54。

但它的新聞檔是 **0 則**。而 MACRO 自己寫的背離條件是
「營收年增為正」且「跌勢**查無**具體利空」。0 則新聞不是查無，是查不到 ——
**前者是證據，後者是證據的缺席**。拿缺席當證據就是 `LESSONS D1` 的單邊採證，
只是這次缺席的那一邊剛好對我有利。本日降為 0.52。

同一天美股那邊出現了對照組：LIN 的 RSI 30.91 為全場第 2 低，
而它有 6 則新聞、**全部是正面的**（Q2 獲利優於預期、KeyBanc 目標價 542 美元）。
那才是「查無具名利空」，所以它由 0.51 升到 0.53。
**兩檔的處置必須不同，差別不在跌幅而在有沒有東西可讀。**

附帶一個反例提醒：IBM 的新聞檔有 6 則，但內容多數與 IBM 無關
（出現 CRL／JCI／Brinker 等）。**「有新聞檔」也不等於「查得到」** ——
筆數不為零只是必要條件。

→ 引用「查無利空」之前，先數該檔新聞筆數，再確認那幾則真的在講這家公司。

## 2026-09-22 ｜部分覆蓋的欄位，比整欄皆空更危險

2026-09-21 那條記的是「盤後 18:00 跑，融資融券整欄是空的」，
對策寫「每天的極端值掃描要先數每一欄的非空筆數」。本日踩到它的下一個形態：

`margin_chg_5`／`margin_chg_20`／`short_ratio` 本日是 **15/50**（不是 0/50）。

**整欄皆空時排序的兩端都是 NaN，一看就知道不能用；部分覆蓋時排序完全正常**，
「margin_chg_20 全場最高」這種句子寫得出來、讀起來也沒有異狀，
而它實際上只是 15 檔裡的最高。更糟的是有值的那 15 檔裡包含台灣大 3045
（`margin_chg_20` +62.2%）—— 它正是 2026-09-19 記載的「基數假象」高風險標的。
兩個陷阱疊在一起，而且都不會拋錯。

→ 判準補強：非空筆數 **< 全體的 90%** 時，該欄一律不得用於極值宣稱，
   並在 MACRO 明講覆蓋率。不是「空的才不能用」，是「不完整就不能排序」。

## 2026-09-22 ｜`finalize` 的 glob 只認一個 as_of，跨市場 as_of 不同的日子會整個市場漏掉

本日台股 as_of 是 **20260922**、美股是 **20260921**（美股當天尚未開盤）。
`daily.py finalize` 的第 97 行是

```python
allj = list((ROOT / "judgments").glob(f"{as_of}*.json"))
```

`as_of` 只有一個值（取自 `_as_of()`，本日為 20260922）。於是它找到 3 份台股檔，
而 **`20260921_us_h20.json`／`_h5.json`／`20260921_us_1y.json` 一份都沒進去** ——
53 檔 × 2 個期別 = 106 筆預測加上 39 筆一年期，全部不會被 ingest，
而 finalize 會照常印出「已提交並推送」。**沒有任何一個環節會報錯。**

這正是 2026-09-21 留下的待辦（「prepare 的收尾訊息應該逐市場印各自的 as_of」）
的另一半：訊息誤導只是症狀，`finalize` 的 glob 才是會真的掉資料的地方。
本日的處置是在 finalize 之前手動
`ingest_judgment.py judgments/20260921_us_*.json`（ingest 以 pid 去重，安全），
之後再跑 finalize，並用 `git show --stat HEAD` 確認六份判斷檔都在 commit 裡。

→ 待辦（比訊息那一半重要）：`finalize` 應逐市場取各自的 as_of 來 glob，
   或至少在「某個市場本日沒有任何判斷檔被選中」時**中止而不是繼續**。
→ 通則：**一個以單一日期為鍵的流程，遇到兩個市場各有自己的日期就會靜默掉資料。**
   本專案已經在 `predict.py` 修過同型問題（2026-09-17 逐檔用自己市場的交易日），
   但 `finalize` 沒有跟上 —— 又一次「同一個概念有兩處實作，錯的那套沒人看」。

## 2026-09-22 ｜「財報更新重算 35 檔」，而財報零變動；連帶讓 9 項人工待辦變成假警報

2026-09-21 記過本益比在基礎雜湊裡、價格天天改它，當天是 36 檔。
本日 35 檔，而且我這次把它查到底：

| 檢查 | 結果 |
|---|---|
| 季報快照（`fundamentals.as_of`，12 欄）9/21 → 9/22 | **0/67 列有變動** |
| 月營收（`checkpoints._monthly_rev`）可用列 | 兩日皆 8,419 列，**0/68 檔有變動** |
| `avail_date == 2026-09-22` 的月營收列數 | **0** |

雜湊的輸入只剩本益比，所以 35 檔**全部**是價格造成的。
台灣大的例子：收盤 124.5 → 123.0，本益比就跨過小數第一位。

**新發現的後果比標籤錯誤嚴重**：`prepare` 印出的「⚠ 11 項待人工重讀」裡，
有 **9 項**是「基礎更新（財報或月營收）　已實查（需人工重寫）」
（2308／2330／2317／2382／3231／3037／2454／3189／2344）。
既然財報零變動，這 9 項**沒有任何新財報可以拿去對前提** —— 全部是假警報。
真正需要讀的只有 2 項新聞事件（3045、2344）。
也就是說這個佇列今天的訊噪比是 2:9，而
「這個機制的全部價值在於亮起來的時候一定有事」（2026-09-19、2026-09-21 各記過一次）。

→ 待辦升級為必須做：把本益比移出基礎雜湊（歸「重新定價」管），
   或把門檻改成相對變動 ±3%，並連帶寫一次遷移。
   在那之前，**看到「基礎更新」要先自己驗季報與月營收有沒有真的變**，
   不要直接開始重讀論點。

## 2026-09-22 ｜會被「已經決定要發生的公司行動」打掉的前提，不是前提

3045 台灣大的一年期原前提是「毛利率 ≥19.2%（獲利結構未惡化）」。
但它正在公開收購精誠，2026-09-21 媒體載「倒數 10 個交易日」——
**10 月初完成，落在窗口（2026Q4–2027Q3）之內**。
精誠自己 FY2026 Q2 的毛利率是 **17%**，低於台灣大的 22.16%，
所以併表會**因為算術**把混合毛利率壓下來，與電信本業惡不惡化無關。

這是 2026-09-19 那條（「檢查點在寫下當天就已經是假的」）的下一個形態：
那次的病是「門檻寫在現值之上」，這次的病是
**門檻會被一個我今天就已經知道、而且已經決定要發生的事件打掉**。
兩者的結果一樣 —— 警報會亮，但亮的原因不是我該怕的那件事。

處置：門檻下修到 18.0%（留出併表稀釋的算術空間），
並補上這筆全現金收購真正會動到的那一項 —— 負債比（現值 61.2%，門檻 68%）。
P漲 維持 0.543 不動（溢價已由宣布時的 38% 收斂到 2.87%，事件本身沒有賠率了）。

**對照**：同一天 2344 華邦電的併購新聞被重複標記，但它的交易 2027 下半年才完成、
落在窗口**之外**，論點 9/18 已寫明這一點，所以「已讀，論點不變」。
**同樣是併購新聞，差別只在完成日在不在窗口內** —— 這是判斷併購類標記的第一個問題。

→ 寫任何檢查點之前問兩件事：(1) 它在基準日是真的嗎（2026-09-19）；
   (2) **有沒有任何已經排定的事件會在窗口內把它打掉**（本條）。

## 2026-09-22 ｜`SEC_UA` 未設，`collect/sec_edgar.py` 每天都在失敗

`prepare` 的摘要裡 `collect/sec_edgar.py ✗ 失敗（returncode 1）`，
原因是 SEC 要求 User-Agent 帶聯絡信箱，否則一律 403。
排程環境沒有設 `SEC_UA` 環境變數。

目前**不影響流程**：美股基本面走 `collect/us_fundamentals.py`（yfinance）且成功，
184 項測試全過。但這一步存在的意義是拿 XBRL 原始數據交叉驗 yfinance，
而它已經連續失敗多日 —— 也就是那道交叉驗證實際上不存在。

→ 與 2026-09-19 記的「universe.py 盤前失敗是防線不是故障」不同：
   那一個是刻意的，這一個是**環境沒設好而靜默降級**。
   判斷方式：失敗訊息裡有沒有寫「拒絕覆寫」這類主動防衛的字。
→ 待辦：請使用者決定要不要提供聯絡信箱設成 `SEC_UA`；
   在那之前不要把這一行當成正常現象忽略掉。
