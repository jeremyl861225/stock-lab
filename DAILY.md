# 每日預測 SOP

給每天執行這個流程的 Claude。每次都是全新 session，沒有前一天的記憶，
所以這份文件必須自足。**照順序做，不要跳步。**

專案：`/Users/jeremy/Desktop/Claude code/stock-lab`
Python：`./.venv/bin/python`（不要用系統 python，會缺套件）

---

## 一、準備（機器做）

```bash
cd "/Users/jeremy/Desktop/Claude code/stock-lab" && ./.venv/bin/python src/daily.py prepare
```

這一步會收台股與美股資料（含美股季報）、建 panel（含除權息還原）、
產四面向簡報與 K 線、跑基準線與統計模型、**滾動兩個市場的一年期論點**、
結算到期預測、跑全套測試。

`prepare` 尾端的一年期滾動會印出三種訊息，**每一種都要看**：

| 印出的內容 | 意思 | 你要做的事 |
|---|---|---|
| `僅重新定價 N 檔` | 財報沒動，只有收盤與波動度變了 | 不用做任何事 |
| `財報更新重算 N 檔` | 有新季報或月營收，規則推導的已自動重算 | 看一眼 P漲 的變化合不合理 |
| `⚠ N 項待人工重讀` | 檢查點被推翻、已實查論點的財報更新、或**出現新聞事件** | **見第二點五節，不可跳過** |

**測試若失敗必須停下來查清楚，不要繼續。** 那些測試在擋的是未來函數、
除權息還原不完整、預測記錄重複 —— 每一項都會讓成績單失真。

## 一點五、回顧（新增，**不可跳過**）

每天都是全新 session，沒有昨天的記憶。所以「學習」不會自動發生 ——
它只存在於下面這幾個檔案裡。做判斷之前必須先讀：

1. **`LESSONS.md`** —— 三輪獨立審核實際查出的錯誤型態清冊。
   每一條都真的發生過，而且多數不會拋錯，只會讓判斷安靜地建立在錯的前提上。
   判斷寫完後，再拿這份清冊逐項自檢一次。

2. **成績單**：`./.venv/bin/python src/accuracy.py`
   以及 `./.venv/bin/python src/attribution.py` —— 後者把成績拆成
   **市場判斷**（錨點對不對）與**選股判斷**（去均值後的橫斷面）。
   不拆的話，準確率量到的幾乎全是錨點，因為手寫 p 的橫斷面標準差只有 0.02–0.03。
   有結算資料時，看期望值加權準確率與幅度校準斜率（斜率遠小於 1 代表幅度灌水）。
   還沒有結算時它會顯示 pending，那是正常的。

3. **上一次的判斷與它的下場**（有結算時）：
   讀 `data/settlements.jsonl` 最近的幾十筆，找**錯得最離譜的幾檔**，
   回頭看 `data/reasoning.jsonl` 裡當時寫的理由與否證條件 ——
   是事實錯了、推論錯了、還是推論對但市場不理會？
   這三種錯誤的處置完全不同，分不清楚就不會進步。

4. **`data/revisions.jsonl` 的最後幾筆** —— 最近一次審核發現了什麼、哪些還沒修。

**若這次回顧發現了新的錯誤型態，追加到 `LESSONS.md` 對應分類，註明日期與案例。**
那份檔案是這個系統唯一會累積的東西。

## 一點七、一年期論點的維護（**有標記才做**）

一年期預測**每天都在更新，但更新的是三件不同的事**，不要混為一談：

| 更新的東西 | 頻率 | 誰做 |
|---|---|---|
| 目標價、保守價、幅度（重新定價） | **每個交易日** | 機器，自動 |
| P漲（重新判斷） | 財報輸入變的那幾天：台股約 16 天／年、美股約 4 天／年 | 機器，僅限規則推導的標的 |
| 論點本身（重寫） | 被標記的時候 | **你**，看完資料再寫 |

第三件事就是這一節。`prepare` 印出 `⚠ N 項待人工重讀` 時，逐項處理：

**A. `新聞事件：併購分拆／財測調整／法規訴訟／經營層異動`**
新聞閘（`src/news_gate.py`）只過濾出財報看不到、而且不等下一季就會改變論點的事。
它**刻意不管財報與月營收** —— 那兩者已經在基礎雜湊裡。
它也**絕不自動改 P漲**：財報規則沒有讀新聞的能力，讓一則標題去改數字，
等於用沒有依據的數字取代有依據的數字，而帳本會把它記成一次新判斷。

所以你要做的是：讀那幾則新聞（標題已經印出來，連結在 `data/raw/news/<as_of>/<code>.json`），
判斷它是否推翻或改變了既有論點的**前提**。
- 沒改變（例如一則評論引用了一個早就知道的事實）→ 在回報裡說一句「已讀，論點不變」。
- 改變了 → 重寫該檔的論點與檢查點，寫成 `judgments/build_1y_<市場>_<as_of>.py`
  （台股省略市場字樣），跑它產出 JSON，再用
  `./.venv/bin/python src/ingest_judgment.py <檔案> --revise` 寫入。
  **舊的那筆不會被刪掉**，修訂以 revision 遞增並存。

新聞閘的精準率大約一半 —— 它的工作是把九百多則標題縮成一兩則給你看，
不是替你判斷。看到不相干的，直接說「已讀，不相干」就好。

**B. `檢查點新增被推翻`**
前提倒了代表論點該重寫，不是把數字調一調。已實查的論點必須由你重寫；
規則推導的會在下一次財報更新時自然反映，但仍要在回報裡點名。

**C. `基礎更新（財報或月營收）` 且標記「已實查」**
新季報出來了，而這一檔的論點是讀法說會寫的 —— 程式不會動它。
你要拿新財報去對原論點的每一條前提，看數字還撐不撐得住。

**沒有標記的日子什麼都不用做。** 每天重寫一年期論點就是把雜訊包裝成更新 ——
h=250 的日頻預測相鄰兩天重疊 249/250，天天改判等於用一年後的結果替雜訊背書。

## 二、判斷（你做，這是唯一無法自動化的部分）

讀 `data/briefing.parquet`（103 檔 × 四面向）：

| 面向 | 欄位 | 台股 | 美股 |
|---|---|---|---|
| 基本面 | PER／PBR／dividend_yield／rev_yoy／rev_yoy3m／rev_mom | ✓ | ✓（季報） |
| 籌碼面 | foreign_5／foreign_20／trust_5／margin_chg_5／margin_chg_20／short_ratio | ✓ | **無** |
| 技術面 | rsi_14／ret_5／ret_20／ret_60／dist_high_60／ma_gap_20／vol_20 | ✓ | ✓ |
| 新聞面 | `data/raw/news/YYYYMMDD/{code}.json` | ✓ | ✓ |
| 事件面 | evt_type／evt_date／evt_days／evt_in_5／evt_in_20／evt_types_20 | ✓ | ✓ |

**事件面是 2026-09-19 新增的，判斷時要真的用它。**
`evt_in_5` / `evt_in_20` 表示下一個事件（台股：月營收、季報法定期限、除權息；
美股：財報日）是否落在該期間的視窗內。視窗內有財報的標的，實現波動與漂移
都與沒有財報的不同 —— 這是 5／20 日最大的遺漏條件變數。
以往只有台積電那種大事會被人記在 `why` 裡，103 檔記不住。
（這些欄位刻意不進統計模型：未來排程是「今天查得到」而非「as_of 查得到」的資訊。）

**先做這三件事，再下判斷：**

1. **查當日大盤實況**，不要用前一天的敘事。曾經犯過的最大錯誤是拿 9/15 的
   恐慌去解釋 9/16 的反彈 —— 當天實際是等權 +1.21%、39/50 檔上漲。
   用系統自己的資料驗：`panel` 最新一日的等權報酬與上漲檔數。
2. **掃極端值**：RSI 最高／最低、營收年增最強／最弱、外資買賣超最極端、
   融資變動最大、距高點最深。背離（價格與基本面反向）是最有價值的訊號。
3. **查重點標的新聞**。跌停或暴漲往往有事件原因 ——
   欣興曾因被搜索／洗產地跌停，卻差點被寫成「需求疲弱」。

**判斷格式**：照 `judgments/build_20260916_v2.py` 的結構寫一支新檔
`judgments/build_<as_of>.py`（台股）與 `judgments/build_us_<as_of>.py`（美股），
每檔給 `(code, p_up, skew, conviction, why)`：

**寫完判斷檔後，要先執行它產生 JSON**（`finalize` 找的是 `.json`，`.py` 不會自己變成 `.json`）：

```bash
cd "/Users/jeremy/Desktop/Claude code/stock-lab"
./.venv/bin/python judgments/build_<as_of>.py
./.venv/bin/python judgments/build_us_<as_of>.py
```

腳本內的 `as_of` 字串與輸出檔名都硬編碼在檔案裡，複製範本後要一起改；
且必須在 repo 根目錄執行（腳本用相對路徑）。

> **`<as_of>` 是「資料的最新交易日」，不是執行日。**
> 早上 7:30 跑的時候 as_of 是前一個交易日（週一還要回到上週五）。
> `prepare` 結束時會直接印出該用哪個日期，照著印出來的命名即可。

- `p_up`：20 日期間的上漲機率。**要校準** —— 你給 0.60 的那批，實際要約 60% 上漲。
  中性錨見 `METHOD.md` §4.3（2026-09-18 起台股 0.53、美股 0.52），沒有明確理由時貼近它。
  （舊版這裡寫「台股 20 日基本率約 0.58–0.68」，那個數字來自一段全程多頭、
  獨立樣本數約等於 1 的樣本，已於 2026-09-18 作廢。）
- **`ANCHOR` 常數與 build 回傳的 `anchor` 欄位必須一起改**：它是整批的共同平移項，
  進了帳本才能事後把「市場判斷錯」與「選股判斷錯」分開歸因。
- `skew`：報酬不對稱。>0 表示上檔大於下檔。幅度由 `0.8 × vol_20 × √h` 自動算，
  **不要手填幅度** —— 手填會讓低波動股的區間開太寬（曾犯，相關係數 −0.756）。
- `conviction`：high／medium／low。美股無籌碼面，信心應普遍低於台股。
- `why`：**每個數字都要能在 briefing 裡對得上**。曾有一輪審核抓到 9 項事實錯誤，
  全部是極值宣稱（「投信買超最強」實為第 7）與歸因錯誤。

5 日的機率由程式自動推導（`(p₅−0.5) = (p₂₀−0.5) × √(5/20)`），不要另外給。

**自動查核**（寫完判斷檔並產生 JSON 之後，進系統之前）：

```bash
./.venv/bin/python src/verify_judgment.py <as_of>
```

它會機械化地驗六件事：數值宣稱對不對得上 briefing、極值宣稱（全場最高／最低）
用排序驗、同一份文件內有無自相矛盾、機率與宣稱的主導維度方向對不對、
與判斷相反的極端值有沒有被略過、high/medium 判斷有沒有新聞可查證。

**目標是歸零。** 有「待確認」就逐項查清楚再改判斷檔重跑 ——
這六類正是四輪人工審核抓到的 30+ 個錯誤的全部來源。

**自我檢查**（自動查核通過後，再逐項對照 `LESSONS.md`）：
- 機率與基本面的相關性方向對嗎？曾經整組金融股的 p 只由 RSI 決定，
  與自己主張的基本面論點相關 −0.234 且方向相反 —— 部位沒有實作論點。
- 有沒有引用「最高／最低／唯一」卻沒驗證的說法？
- 否證條件寫了嗎？要寫「什麼證據會證明我錯」，不是場面話。

## 三、收尾（機器做）

```bash
cd "/Users/jeremy/Desktop/Claude code/stock-lab" && ./.venv/bin/python src/daily.py finalize
```

寫入判斷、產生面板、跑測試、commit 並 push。
面板：https://jeremyl861225.github.io/stock-lab/

## 四、回報給使用者

用繁體中文，簡短講四件事：
1. 今天台股與美股各自期望值最高／最低的三檔，以及理由一句話
2. 今天發現的任何資料異常或事實矛盾（沒有就說沒有）
3. 若有預測到期結算，報加權準確率
4. **一年期今天做了什麼**：是「僅重新定價」、「N 檔因財報更新重算 P漲」，
   還是「重寫了某檔的論點（原因）」。三者要分清楚 ——
   讀者若以為每天都有新判斷，就會把重新定價當成新資訊。

**不要吹捧模型。** 這套系統的價值在於誠實，不在於看起來準。

---

## 已知陷阱

- **TWSE `www.twse.com.tw/rwd/` 限流極嚴**：307 → 428 → IP 冷凍。歷史一律走 FinMind。
- **FinMind 免費版每小時有請求上限**，抓 300 個就會撞到。已快取的不會重抓。
- **除權息必須還原，且價格與股數兩邊都要**：價格乘 factor、股數除以 factor。
- **月營收 `create_time` 舊資料是空字串**，缺值時用法定公告期限（次月 10 日）推定。
- **Google News 只查公司名會抓到八卦**，必須加財經關鍵字。
- **生存者偏差尚未修**（見 README）：universe 是「今天」的市值前 50，
  這批股票兩年等權 +272%。回測數字不能當證據，前瞻成績才算數。
